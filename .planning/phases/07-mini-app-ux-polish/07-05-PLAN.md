---
phase: 07-mini-app-ux-polish
plan: 05
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - frontend/src/pages/master/Dashboard.tsx
  - frontend/src/pages/master/Services.tsx
  - frontend/src/pages/master/Schedule.tsx
  - frontend/src/pages/master/Bookings.tsx
  - frontend/src/pages/master/Clients.tsx
  - frontend/src/pages/master/ClientDetail.tsx
  - frontend/src/pages/master/Settings.tsx
  - frontend/src/pages/master/PaymentHistory.tsx
autonomous: true
requirements:
  - MACC-02
  - MMUX-04

must_haves:
  truths:
    - "All 8 master-panel pages show a meaningful Russian error state when the API call fails"
    - "All async list containers have aria-live=polite for screen reader announcements"
    - "Error states use the existing EmptyState component with a relevant error message"
  artifacts:
    - path: "frontend/src/pages/master/Dashboard.tsx"
      provides: "Error state + aria-live on booking list"
      contains: "aria-live"
    - path: "frontend/src/pages/master/Services.tsx"
      provides: "Error state + aria-live on services list"
      contains: "aria-live"
    - path: "frontend/src/pages/master/Bookings.tsx"
      provides: "Error state + aria-live on bookings list"
      contains: "aria-live"
    - path: "frontend/src/pages/master/Clients.tsx"
      provides: "Error state + aria-live on clients list"
      contains: "aria-live"
    - path: "frontend/src/pages/master/Schedule.tsx"
      provides: "Error state + aria-live"
      contains: "aria-live"
    - path: "frontend/src/pages/master/ClientDetail.tsx"
      provides: "Error state + aria-live"
      contains: "aria-live"
    - path: "frontend/src/pages/master/Settings.tsx"
      provides: "Error state + aria-live"
      contains: "aria-live"
    - path: "frontend/src/pages/master/PaymentHistory.tsx"
      provides: "Error state + aria-live"
      contains: "aria-live"
  key_links:
    - from: "all 8 master-panel pages"
      to: "EmptyState component"
      via: "error condition renders EmptyState with error heading/body"
      pattern: "error.*EmptyState"
---

<objective>
Add error state handling and aria-live announcements to all 8 master-panel pages.

Purpose: When an API call fails, users currently see a blank screen or infinite spinner. Every page needs a meaningful Russian error message. Screen reader users need aria-live regions so async content changes are announced.
Output: All 8 master pages handle error states gracefully and have accessible live regions.
</objective>

<execution_context>
@/Users/yannovak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yannovak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-mini-app-ux-polish/07-01-SUMMARY.md
</context>

<interfaces>
From frontend/src/components/ui/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  icon?: ReactNode;
  heading: string;
  body?: string;
  action?: ReactNode;
}
export function EmptyState({ icon, heading, body, action }: EmptyStateProps)
```

Query hook pattern used across all pages:
```typescript
const { data, isLoading, error } = useXxx();
```
Currently most master pages only destructure `data` and `isLoading`, not `error`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add error states and aria-live to Dashboard, Services, Schedule, Bookings</name>
  <files>frontend/src/pages/master/Dashboard.tsx, frontend/src/pages/master/Services.tsx, frontend/src/pages/master/Schedule.tsx, frontend/src/pages/master/Bookings.tsx</files>
  <read_first>frontend/src/pages/master/Dashboard.tsx, frontend/src/pages/master/Services.tsx, frontend/src/pages/master/Schedule.tsx, frontend/src/pages/master/Bookings.tsx</read_first>
  <action>
Apply the same pattern to each of the 4 pages:

**Pattern for each page:**

1. **Destructure `error` from the query hook** (add `error` to the destructuring)
2. **Add `aria-live="polite"` to the list container div** (the div that wraps the loading/empty/data states)
3. **Add error state rendering** between the loading skeleton and the empty/data rendering

**Dashboard.tsx:**

1. Change line 16: `const { data, isLoading } = useMasterBookings({` to `const { data, isLoading, error } = useMasterBookings({`
2. On the `<div className="flex-1 px-4 pb-4">` (line 43), add `aria-live="polite"`:
   ```tsx
   <div className="flex-1 px-4 pb-4" aria-live="polite">
   ```
3. Add error state after the loading block (after line 49, before the `sorted.length === 0` check):
   ```tsx
   ) : error ? (
     <EmptyState
       icon={<CalendarDays className="w-12 h-12" />}
       heading="Не удалось загрузить записи"
       body="Проверьте соединение и попробуйте ещё раз."
     />
   ```
4. Import `CalendarDays` is already imported. Import `EmptyState` if not already (check: it IS already imported on line 5).

**Services.tsx:**

1. Change `const { data: services, isLoading } = useMasterServices();` to `const { data: services, isLoading, error } = useMasterServices();`
2. Add `aria-live="polite"` to `<div className="flex-1 px-4 pb-4">` (line 50)
3. Add error state after loading block (after line 56, before the `!services || services.length === 0` check):
   ```tsx
   ) : error ? (
     <EmptyState
       icon={<Scissors className="w-12 h-12" />}
       heading="Не удалось загрузить услуги"
       body="Проверьте соединение и попробуйте ещё раз."
     />
   ```

**Schedule.tsx:**

1. Change the schedule query: add `error: scheduleError` to `useMasterSchedule()` destructuring, and `error: exceptionsError` to `useScheduleExceptions()` destructuring.
2. Add `const hasError = !!scheduleError || !!exceptionsError;` after `const isLoading = ...` line.
3. Add `aria-live="polite"` to the `<div className="flex-1 px-4 pb-4 flex flex-col gap-4">` (line 114)
4. After the loading check (line 121), add:
   ```tsx
   ) : hasError ? (
     <EmptyState
       icon={<CalendarDays className="w-12 h-12" />}
       heading="Не удалось загрузить расписание"
       body="Проверьте соединение и попробуйте ещё раз."
     />
   ```
   Import `EmptyState` and a suitable icon (add import for `EmptyState` from `../../components/ui/EmptyState.tsx` and `CalendarDays` from `lucide-react`).

**Bookings.tsx:**

1. Change `const { data, isLoading } = useMasterBookings({` to `const { data, isLoading, error } = useMasterBookings({`
2. Add `aria-live="polite"` to `<div className="flex-1 px-4 pb-4">` (line 124)
3. After the loading block (after line 131, before `bookings.length === 0`):
   ```tsx
   ) : error ? (
     <EmptyState
       icon={<BookOpen className="w-12 h-12" />}
       heading="Не удалось загрузить записи"
       body="Проверьте соединение и попробуйте ещё раз."
     />
   ```
   `BookOpen` and `EmptyState` are already imported.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm && grep -q 'aria-live="polite"' frontend/src/pages/master/Dashboard.tsx && grep -q 'aria-live="polite"' frontend/src/pages/master/Services.tsx && grep -q 'aria-live="polite"' frontend/src/pages/master/Schedule.tsx && grep -q 'aria-live="polite"' frontend/src/pages/master/Bookings.tsx && grep -q "Не удалось загрузить" frontend/src/pages/master/Dashboard.tsx && grep -q "Не удалось загрузить" frontend/src/pages/master/Services.tsx && grep -q "Не удалось загрузить" frontend/src/pages/master/Schedule.tsx && grep -q "Не удалось загрузить" frontend/src/pages/master/Bookings.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files have aria-live="polite" on the list container
    - All 4 files destructure `error` from their query hooks
    - All 4 files render EmptyState with Russian error heading when error is truthy
    - grep "Не удалось загрузить" returns matches in all 4 files
  </acceptance_criteria>
  <done>Dashboard, Services, Schedule, Bookings all have error states and aria-live regions</done>
</task>

<task type="auto">
  <name>Task 2: Add error states and aria-live to Clients, ClientDetail, Settings, PaymentHistory</name>
  <files>frontend/src/pages/master/Clients.tsx, frontend/src/pages/master/ClientDetail.tsx, frontend/src/pages/master/Settings.tsx, frontend/src/pages/master/PaymentHistory.tsx</files>
  <read_first>frontend/src/pages/master/Clients.tsx, frontend/src/pages/master/ClientDetail.tsx, frontend/src/pages/master/Settings.tsx, frontend/src/pages/master/PaymentHistory.tsx</read_first>
  <action>
Apply the same pattern to each of the 4 pages:

**Clients.tsx:**

1. Change `const { data: clients, isLoading } = useMasterClients();` to `const { data: clients, isLoading, error } = useMasterClients();`
2. Add `aria-live="polite"` to `<div className="flex-1 px-4 pb-4">` (line 30)
3. After loading block (after line 36, before `!clients || clients.length === 0`):
   ```tsx
   ) : error ? (
     <EmptyState
       icon={<Users className="w-12 h-12" />}
       heading="Не удалось загрузить клиентов"
       body="Проверьте соединение и попробуйте ещё раз."
     />
   ```
   `Users` and `EmptyState` are already imported.

**ClientDetail.tsx:**

1. Change `const { data, isLoading } = useClientDetail(id);` to `const { data, isLoading, error } = useClientDetail(id);`
2. Add `aria-live="polite"` to `<div className="flex-1 px-4 pb-4">` (line 37)
3. After loading block (after line 43, before `data ?`):
   ```tsx
   ) : error ? (
     <EmptyState
       icon={<User className="w-12 h-12" />}
       heading="Не удалось загрузить данные клиента"
       body="Проверьте соединение и попробуйте ещё раз."
     />
   ```
   Import `EmptyState` from `../../components/ui/EmptyState.tsx` (not currently imported). `User` is already imported.

**Settings.tsx:**

1. Add `error` destructuring to ALL THREE hooks:
   - `const { data: settings, isLoading, error: settingsError } = useMasterSettings();`
   - `const { data: paymentSettings, isLoading: paymentLoading, error: paymentError } = usePaymentSettings();`
   - `const { data: notificationSettings, isLoading: notifLoading, error: notifError } = useNotificationSettings();`
2. Add `const hasError = !!settingsError || !!paymentError || !!notifError;` after the `isLoadingAny` line.
3. Add `aria-live="polite"` to `<div className="flex-1 px-4 pb-4 flex flex-col gap-4">` (line 239)
4. After the loading block (after line 248, before the `<>` fragment), add error state:
   ```tsx
   ) : hasError ? (
     <EmptyState
       icon={<Settings className="w-12 h-12" />}
       heading="Не удалось загрузить настройки"
       body="Проверьте соединение и попробуйте ещё раз."
     />
   ```
   Import `EmptyState` from `../../components/ui/EmptyState.tsx` (not currently imported). Need to also import a suitable icon. But `Settings` is the component name itself. Use a different icon. Import `AlertCircle` from `lucide-react`:
   ```tsx
   import { ..., AlertCircle } from "lucide-react";
   ```
   Then use `<AlertCircle className="w-12 h-12" />` as the icon.

**PaymentHistory.tsx:**

1. Change `const { data, isLoading } = usePaymentHistory({` to `const { data, isLoading, error } = usePaymentHistory({`
2. Add `aria-live="polite"` to `<div className="flex-1 px-4 pb-4">` (line 143)
3. After loading block (after line 150, before `payments.length === 0`):
   ```tsx
   ) : error ? (
     <EmptyState
       icon={<CreditCard className="w-12 h-12" />}
       heading="Не удалось загрузить платежи"
       body="Проверьте соединение и попробуйте ещё раз."
     />
   ```
   `CreditCard` and `EmptyState` are already imported.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm && grep -q 'aria-live="polite"' frontend/src/pages/master/Clients.tsx && grep -q 'aria-live="polite"' frontend/src/pages/master/ClientDetail.tsx && grep -q 'aria-live="polite"' frontend/src/pages/master/Settings.tsx && grep -q 'aria-live="polite"' frontend/src/pages/master/PaymentHistory.tsx && grep -q "Не удалось загрузить" frontend/src/pages/master/Clients.tsx && grep -q "Не удалось загрузить" frontend/src/pages/master/ClientDetail.tsx && grep -q "Не удалось загрузить" frontend/src/pages/master/Settings.tsx && grep -q "Не удалось загрузить" frontend/src/pages/master/PaymentHistory.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files have aria-live="polite" on the list/content container
    - All 4 files destructure `error` from their query hooks
    - All 4 files render EmptyState with Russian error heading when error is truthy
    - grep "Не удалось загрузить" returns matches in all 4 files
    - ClientDetail.tsx imports EmptyState (was not imported before)
    - Settings.tsx imports AlertCircle and EmptyState
  </acceptance_criteria>
  <done>Clients, ClientDetail, Settings, PaymentHistory all have error states and aria-live regions; all 8 master-panel pages now covered</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- All 8 files in frontend/src/pages/master/ contain `aria-live="polite"`
- All 8 files contain error handling with Russian error messages
- `grep -r 'aria-live="polite"' frontend/src/pages/master/` returns 8 matches
- `grep -r "Не удалось загрузить" frontend/src/pages/master/` returns 8 matches
</verification>

<success_criteria>
- A screen reader announces content changes when data loads or errors occur on any master-panel page
- When any API call fails, the user sees a Russian error message ("Не удалось загрузить...") instead of a blank screen or infinite spinner
- All 8 pages covered: Dashboard, Services, Schedule, Bookings, Clients, ClientDetail, Settings, PaymentHistory
</success_criteria>

<output>
After completion, create `.planning/phases/07-mini-app-ux-polish/07-05-SUMMARY.md`
</output>
