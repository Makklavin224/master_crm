---
phase: 20-error-handling-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/api/client.ts
  - frontend/src/api/client.ts
  - public/src/api/client.ts
  - frontend/src/stores/master-auth.ts
  - web/src/stores/auth.ts
  - web/src/pages/LoginPage.tsx
autonomous: true
requirements: [ERRH-02, ERRH-03]

must_haves:
  truths:
    - "API calls that take longer than 30 seconds abort with a timeout error instead of spinning forever"
    - "All user-facing error messages across all three frontends display in Russian"
  artifacts:
    - path: "web/src/api/client.ts"
      provides: "Web admin API client with AbortController timeout and Russian errors"
      contains: "AbortController"
    - path: "frontend/src/api/client.ts"
      provides: "Mini-app API client with AbortController timeout"
      contains: "AbortController"
    - path: "public/src/api/client.ts"
      provides: "Public site API client with AbortController timeout"
      contains: "AbortController"
    - path: "frontend/src/stores/master-auth.ts"
      provides: "Master auth store with AbortController timeout and Russian errors"
      contains: "AbortController"
    - path: "web/src/stores/auth.ts"
      provides: "Web auth store with Russian error messages"
    - path: "web/src/pages/LoginPage.tsx"
      provides: "Login page with Russian error messages"
  key_links:
    - from: "web/src/api/client.ts"
      to: "all web admin API hooks"
      via: "apiRequest function used by all hooks"
      pattern: "AbortController"
    - from: "frontend/src/api/client.ts"
      to: "all mini-app API hooks"
      via: "apiRequest function used by all hooks"
      pattern: "AbortController"
---

<objective>
Add 30-second AbortController timeout to all API client functions across all three frontends (web admin, mini-app, public site), and replace all remaining English error messages with Russian equivalents.

Purpose: Prevent infinite spinners when the backend is slow or unresponsive, and ensure consistent Russian-language error UX across all surfaces.
Output: 6 updated files with timeout and Russian error strings.
</objective>

<execution_context>
@/Users/yannovak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yannovak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AbortController 30s timeout to all 4 API request functions</name>
  <files>web/src/api/client.ts, frontend/src/api/client.ts, public/src/api/client.ts, frontend/src/stores/master-auth.ts</files>
  <read_first>web/src/api/client.ts, frontend/src/api/client.ts, public/src/api/client.ts, frontend/src/stores/master-auth.ts</read_first>
  <action>
    Add a 30-second AbortController timeout to each of the 4 API request functions. The pattern for each:

    1. **web/src/api/client.ts** (`apiRequest`):
       - At the top of the function, create `const controller = new AbortController();` and `const timeoutId = setTimeout(() => controller.abort(), 30_000);`
       - Pass `signal: controller.signal` into the fetch options: `fetch(url, { ...options, headers, signal: controller.signal })`
       - In a `finally` block, call `clearTimeout(timeoutId)`
       - In the catch block, detect AbortError: `if (error instanceof DOMException && error.name === 'AbortError') throw new ApiError(0, 'Превышено время ожидания. Попробуйте позже.');`
       - If the caller already passes a `signal` in options, merge them: respect the caller's signal by also aborting if caller's signal fires. Simple approach: if `options.signal` exists, use `AbortSignal.any([options.signal, controller.signal])` as the signal (supported in all modern browsers). If `AbortSignal.any` is not available (check with a typeof guard), fall back to just `controller.signal`.

    2. **frontend/src/api/client.ts** (`apiRequest`):
       - Same pattern as #1. This function also accepts `initDataRaw` as 3rd param -- keep that signature intact.
       - Timeout error message: `'Превышено время ожидания. Попробуйте позже.'`

    3. **public/src/api/client.ts** (`apiRequest`):
       - Same pattern as #1.
       - Timeout error message: `'Превышено время ожидания. Попробуйте позже.'`

    4. **frontend/src/stores/master-auth.ts** (`masterApiRequest`):
       - Same pattern as #1. This function reads token from `useMasterAuth.getState()` -- keep that intact.
       - Timeout error message: `'Превышено время ожидания. Попробуйте позже.'`

    Do NOT add timeout to the raw fetch calls in web/src/stores/auth.ts or LoginPage.tsx -- those are one-off auth calls that will be handled in Task 2.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm && grep -c "AbortController" web/src/api/client.ts frontend/src/api/client.ts public/src/api/client.ts frontend/src/stores/master-auth.ts | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>All 4 files contain AbortController. Each apiRequest/masterApiRequest creates a 30s timeout controller, passes signal to fetch, clears timeout in finally, and catches AbortError with a Russian message.</acceptance_criteria>
  <done>All 4 API client functions abort after 30 seconds with Russian timeout error message</done>
</task>

<task type="auto">
  <name>Task 2: Replace all English error messages with Russian across all frontends</name>
  <files>web/src/api/client.ts, web/src/stores/auth.ts, web/src/pages/LoginPage.tsx, frontend/src/stores/master-auth.ts</files>
  <read_first>web/src/api/client.ts, web/src/stores/auth.ts, web/src/pages/LoginPage.tsx, frontend/src/stores/master-auth.ts</read_first>
  <action>
    Replace every English-language error string with its Russian equivalent:

    1. **web/src/api/client.ts** line 34:
       - Change `.catch(() => ({ detail: "Network error" }))` to `.catch(() => ({ detail: "Ошибка сети" }))`

    2. **web/src/stores/auth.ts** line 86:
       - Change `.catch(() => ({ detail: "Login failed" }))` to `.catch(() => ({ detail: "Не удалось войти" }))`

    3. **web/src/stores/auth.ts** line 97:
       - Change `error: "Network error"` to `error: "Ошибка сети"`

    4. **web/src/pages/LoginPage.tsx** line 110:
       - Change `throw new Error("Failed to create QR session")` to `throw new Error("Не удалось создать QR-сессию")`
       - Note: this error is caught on line 118-119 and displayed as "Не удалось создать QR-код" to the user, so the thrown message is internal. But standardize it to Russian anyway for consistency.

    5. **frontend/src/stores/master-auth.ts** line 209:
       - Change `.catch(() => ({ detail: "Network error" }))` to `.catch(() => ({ detail: "Ошибка сети" }))`

    Also verify that `frontend/src/api/client.ts` and `public/src/api/client.ts` already use Russian ("Ошибка сети") -- they do, no changes needed there.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm && grep -rn '"Network error"\|"Login failed"\|"Failed to create QR"' web/src/ frontend/src/ public/src/ --include="*.ts" --include="*.tsx" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>The grep returns 0 -- zero English error messages remain in any frontend source file. All error strings visible to users or in catch blocks are in Russian.</acceptance_criteria>
  <done>Zero English error messages remain across all three frontends</done>
</task>

</tasks>

<verification>
1. `grep -rn "AbortController" web/src/api/client.ts frontend/src/api/client.ts public/src/api/client.ts frontend/src/stores/master-auth.ts` -- all 4 files have AbortController
2. `grep -rn '"Network error"\|"Login failed"\|"Failed to"' web/src/ frontend/src/ public/src/ --include="*.ts" --include="*.tsx"` -- returns empty (no English errors)
3. `grep -rn "Превышено время ожидания" web/src/api/client.ts frontend/src/api/client.ts public/src/api/client.ts frontend/src/stores/master-auth.ts` -- all 4 files have Russian timeout message
</verification>

<success_criteria>
- All 4 API request functions have 30-second AbortController timeout
- Timeout produces Russian error message "Превышено время ожидания. Попробуйте позже."
- All English error messages replaced with Russian equivalents
- No functional regressions in API request behavior
</success_criteria>

<output>
After completion, create `.planning/phases/20-error-handling-ux/20-01-SUMMARY.md`
</output>
