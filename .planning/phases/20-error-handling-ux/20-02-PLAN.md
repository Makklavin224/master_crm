---
phase: 20-error-handling-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/pages/ReviewsPage.tsx
  - web/src/pages/SettingsPage.tsx
autonomous: true
requirements: [ERRH-01, ERRH-04, ERRH-05]

must_haves:
  truths:
    - "ReviewsPage shows an error state with a retry button when the API fails"
    - "Card number input in payment settings auto-formats as XXXX XXXX XXXX XXXX"
    - "Robokassa test mode displays a prominent warning banner, not just a small badge"
  artifacts:
    - path: "web/src/pages/ReviewsPage.tsx"
      provides: "Reviews page with error handling via isError + Result/retry"
      contains: "isError"
    - path: "web/src/pages/SettingsPage.tsx"
      provides: "Card number formatting and Robokassa test mode Alert"
      contains: "Alert"
  key_links:
    - from: "web/src/pages/ReviewsPage.tsx"
      to: "web/src/api/reviews.ts"
      via: "useReviews hook returns isError"
      pattern: "isError"
    - from: "web/src/pages/SettingsPage.tsx"
      to: "Antd Alert component"
      via: "Robokassa test mode warning"
      pattern: "Alert.*тестовый"
---

<objective>
Add error handling to ReviewsPage (error state + retry), format card number input as XXXX XXXX XXXX XXXX in payment settings, and replace the small Robokassa test mode Badge with a prominent Alert warning banner.

Purpose: Ensure every admin page handles API errors gracefully, payment card input is user-friendly, and test mode is unmistakably visible to prevent confusion with real payments.
Output: 2 updated files in web admin.
</objective>

<execution_context>
@/Users/yannovak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yannovak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error handling to ReviewsPage with retry button</name>
  <files>web/src/pages/ReviewsPage.tsx</files>
  <read_first>web/src/pages/ReviewsPage.tsx, web/src/api/reviews.ts</read_first>
  <action>
    Add error handling to ReviewsPage so that API failures show a user-friendly error state instead of a blank page.

    1. In the `useReviews` destructure on line 48, add `isError` and `refetch`:
       `const { data, isLoading, isError, refetch } = useReviews({...})`

    2. Add an `import { Result } from "antd"` (Result is already available from antd but not imported -- add it to the existing antd import block on line 2).

    3. After the columns definition and before the `return` statement, add an early return for the error state:
       ```tsx
       if (isError) {
         return (
           <Card title="Отзывы">
             <Result
               status="error"
               title="Ошибка загрузки"
               subTitle="Не удалось загрузить отзывы. Попробуйте ещё раз."
               extra={
                 <Button type="primary" onClick={() => refetch()}>
                   Повторить
                 </Button>
               }
             />
           </Card>
         );
       }
       ```

    4. Place this error check BEFORE the main return (around line 156, after columns definition). This ensures the Card wrapper is still rendered for visual consistency with the rest of the admin layout.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm && grep -c "isError" web/src/pages/ReviewsPage.tsx | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>grep returns at least 2 (destructure + conditional check). ReviewsPage renders a Result component with "Ошибка загрузки" title, Russian subtitle, and a "Повторить" button that calls refetch() when isError is true.</acceptance_criteria>
  <done>ReviewsPage shows error state with Russian message and retry button when API fails</done>
</task>

<task type="auto">
  <name>Task 2: Card number formatting and Robokassa test mode warning</name>
  <files>web/src/pages/SettingsPage.tsx</files>
  <read_first>web/src/pages/SettingsPage.tsx (lines 690-800 for payment section)</read_first>
  <action>
    Two fixes in the SettingsPage payment section:

    **Fix A: Card number formatting (XXXX XXXX XXXX XXXX)**

    On line 702, the card_number Form.Item currently uses a plain `<Input>`. Replace it with an Input that auto-formats as the user types:

    1. Add an `onChange` handler to the card_number Input that strips non-digits and inserts spaces every 4 chars:
       ```tsx
       <Form.Item name="card_number" label="Номер карты">
         <Input
           placeholder="0000 0000 0000 0000"
           maxLength={19}
           onChange={(e) => {
             const digits = e.target.value.replace(/\D/g, '').slice(0, 16);
             const formatted = digits.replace(/(\d{4})(?=\d)/g, '$1 ');
             requisitesForm.setFieldValue('card_number', formatted);
           }}
         />
       </Form.Item>
       ```

    2. Also update the save handler for card_number (`handleSaveRequisites` around line 633) to strip spaces before sending to API:
       - Where `card_number: values.card_number || null` is set, change to `card_number: values.card_number?.replace(/\s/g, '') || null`

    3. Update the form initialization (around line 622) to format the existing value from API:
       - Where `card_number: data.card_number ?? ""` is set, change to format it:
         `card_number: data.card_number ? data.card_number.replace(/(\d{4})(?=\d)/g, '$1 ') : ""`

    **Fix B: Robokassa test mode warning**

    On lines 743-745, the test mode indicator is currently a small `<Badge status="warning" text="тестовый режим" />`. Replace this with a prominent `Alert` banner:

    1. Replace the entire test mode section (lines 740-745) with:
       ```tsx
       {data.has_robokassa ? (
         <div>
           <Badge status="success" text="Подключена" />
           {data.robokassa_is_test && (
             <Alert
               type="warning"
               showIcon
               message="Тестовый режим"
               description="Платежи не обрабатываются. Для приёма реальных платежей отключите тестовый режим в настройках Робокассы и переподключите интеграцию."
               style={{ marginTop: 12 }}
             />
           )}
         </div>
       ```

    2. `Alert` is already imported from antd in the SettingsPage imports (line 3). Verify it is there; if not, add it.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm && grep -c "replace.*\\\\D" web/src/pages/SettingsPage.tsx && grep -c "Тестовый режим" web/src/pages/SettingsPage.tsx</automated>
  </verify>
  <acceptance_criteria>Card number input strips non-digits and formats as groups of 4 with spaces. Saved value to API has spaces stripped. Robokassa test mode shows a full Alert component with type="warning", showIcon, and a descriptive Russian message about payments not being processed.</acceptance_criteria>
  <done>Card number auto-formats as XXXX XXXX XXXX XXXX and Robokassa test mode shows a prominent yellow Alert warning banner</done>
</task>

</tasks>

<verification>
1. `grep "isError" web/src/pages/ReviewsPage.tsx` -- ReviewsPage handles API errors
2. `grep "replace.*\\D" web/src/pages/SettingsPage.tsx` -- Card number formatting strips non-digits
3. `grep "Alert.*warning\|Тестовый режим.*Платежи" web/src/pages/SettingsPage.tsx` -- Robokassa warning is an Alert, not a Badge
4. All three frontends build without TypeScript errors: `cd web && npx tsc --noEmit`
</verification>

<success_criteria>
- ReviewsPage shows Result error state with "Повторить" button when API fails
- Card number input formats as XXXX XXXX XXXX XXXX while typing
- Card number saved to API without spaces
- Robokassa test mode shows prominent Alert banner with explanation
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-error-handling-ux/20-02-SUMMARY.md`
</output>
