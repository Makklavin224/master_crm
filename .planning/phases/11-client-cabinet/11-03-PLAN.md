---
phase: 11-client-cabinet
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - public/src/stores/auth.ts
  - public/src/api/client-cabinet.ts
  - public/src/api/types.ts
  - public/src/pages/cabinet/LoginPage.tsx
  - public/src/pages/cabinet/BookingsPage.tsx
  - public/src/components/LoginForm.tsx
  - public/src/components/BookingCard.tsx
  - public/src/components/ReviewForm.tsx
  - public/src/App.tsx
autonomous: true
requirements: [CCAB-01, CCAB-03, CCAB-04, CCAB-05, CCAB-06, CCAB-07]

must_haves:
  truths:
    - "Client enters phone at /my, receives OTP via messenger, enters code, sees bookings list"
    - "OTP form shows 6-digit input with countdown timer (60s) and resend button"
    - "Client sees upcoming bookings with cancel/reschedule actions"
    - "Client sees past bookings with rebook and review actions"
    - "Rebook navigates to /m/{username}/book?service={id} with pre-filled service"
    - "Client can leave 1-5 star review with optional text on completed visits"
    - "Bookings from all masters appear in one list grouped by date"
    - "Clicking master name navigates to /m/{username}"
  artifacts:
    - path: "public/src/stores/auth.ts"
      provides: "Zustand auth store with session state and login/logout"
      exports: ["useAuth"]
    - path: "public/src/api/client-cabinet.ts"
      provides: "API functions for OTP auth, bookings, reviews"
      exports: ["requestOTP", "verifyOTP", "getBookings", "createReview"]
    - path: "public/src/pages/cabinet/LoginPage.tsx"
      provides: "Login page with phone + OTP input"
      min_lines: 40
    - path: "public/src/pages/cabinet/BookingsPage.tsx"
      provides: "Bookings list with upcoming/past sections"
      min_lines: 60
    - path: "public/src/components/LoginForm.tsx"
      provides: "Phone input + OTP verification form"
      min_lines: 60
    - path: "public/src/components/BookingCard.tsx"
      provides: "Booking card with action buttons"
      min_lines: 40
    - path: "public/src/components/ReviewForm.tsx"
      provides: "Star rating + text review form"
      min_lines: 40
  key_links:
    - from: "public/src/stores/auth.ts"
      to: "/api/v1/client/auth/verify-code"
      via: "cookie set by backend, store tracks isAuthenticated"
      pattern: "isAuthenticated"
    - from: "public/src/pages/cabinet/BookingsPage.tsx"
      to: "/api/v1/client/bookings"
      via: "useQuery fetch with credentials"
      pattern: "client/bookings"
    - from: "public/src/components/BookingCard.tsx"
      to: "/m/{username}/book"
      via: "navigate for rebook action"
      pattern: "/m/"
    - from: "public/src/components/ReviewForm.tsx"
      to: "/api/v1/client/reviews"
      via: "POST mutation"
      pattern: "client/reviews"
    - from: "public/src/App.tsx"
      to: "public/src/pages/cabinet/LoginPage.tsx"
      via: "Route path=/my"
      pattern: "/my"
---

<objective>
Build the frontend client cabinet: login form with phone + OTP, bookings list (upcoming + past), rebook/cancel/reschedule actions, and review form.

Purpose: This is the user-facing part of the client cabinet. After the backend API (Plan 01) and routing fix (Plan 02), this plan implements the actual UI clients interact with at /my.

Output: Auth store, API module, login page, bookings page, 3 reusable components (LoginForm, BookingCard, ReviewForm), App.tsx route additions.
</objective>

<execution_context>
@/Users/yannovak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yannovak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-client-cabinet/11-01-SUMMARY.md
@.planning/phases/11-client-cabinet/11-02-SUMMARY.md
@.planning/phases/10-public-master-page/10-03-SUMMARY.md

<interfaces>
<!-- Backend API contracts from Plan 01 -->

POST /api/v1/client/auth/request-code
  Request: { "phone": "+79161234567" }
  Response 200: { "success": true, "message": "Код отправлен", "cooldown_seconds": null }
  Response 429: { "detail": "..." } (cooldown active)
  Response 404: { "detail": "Клиент не найден..." }

POST /api/v1/client/auth/verify-code
  Request: { "phone": "+79161234567", "code": "123456" }
  Response 200: { "token": "abc..." } + Set-Cookie: client_session=abc...; HttpOnly; Secure; SameSite=Lax; Max-Age=604800
  Response 400: { "detail": "Неверный код. Осталось попыток: 2" }

GET /api/v1/client/bookings (requires cookie auth)
  Response 200: {
    "upcoming": [{ "id": "uuid", "master_id": "uuid", "master_name": "Анна", "service_id": "uuid", "service_name": "Маникюр", "starts_at": "2026-03-25T14:00:00+03:00", "ends_at": "...", "status": "confirmed", "source_platform": "web", "master_username": "anna" }],
    "past": [...]
  }

POST /api/v1/client/reviews (requires cookie auth)
  Request: { "booking_id": "uuid", "rating": 5, "text": "Отлично!" }
  Response 201: { "id": "uuid", "rating": 5, "text": "Отлично!", "created_at": "..." }
  Response 409: { "detail": "Вы уже оставили отзыв..." }
  Response 400: { "detail": "Отзыв можно оставить только после завершённого визита" }

Existing cancel endpoint (no auth needed for client path):
PUT /api/v1/bookings/{id}/cancel
  Request: { "reason": "..." }

Existing reschedule endpoint:
PUT /api/v1/bookings/{id}/reschedule
  Request: { "new_starts_at": "2026-03-26T15:00:00+03:00" }

<!-- Frontend patterns from existing codebase -->

From public/src/api/client.ts:
```typescript
const API_BASE = import.meta.env.VITE_API_BASE_URL || "/api/v1";
export async function apiRequest<T>(path: string, options: RequestInit = {}): Promise<T>
export class ApiError extends Error { status: number; detail: string; }
```

From public/src/stores/booking-flow.ts (pattern for Zustand store):
```typescript
import { create } from "zustand";
export const useBookingFlow = create<BookingFlowState>((set) => ({ ... }));
```

From public/src/index.css (Tailwind theme tokens):
- Colors: text-primary, text-secondary, bg-surface, bg-white, primary, primary-dark
- Font: font-sans (Inter)
- Shadows: shadow-sm, shadow-md

From public/src/api/types.ts (existing types to reference):
```typescript
export interface BookingRead { id, master_id, client_id, service_id, starts_at, ends_at, status, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth store, API module, types, and LoginForm component</name>
  <files>public/src/stores/auth.ts, public/src/api/client-cabinet.ts, public/src/api/types.ts, public/src/components/LoginForm.tsx, public/src/pages/cabinet/LoginPage.tsx</files>
  <action>
1. **Add types to public/src/api/types.ts** (append to existing file):
   ```typescript
   export interface OTPResponse {
     success: boolean;
     message: string;
     cooldown_seconds?: number;
   }

   export interface SessionResponse {
     token: string;
   }

   export interface ClientBookingRead {
     id: string;
     master_id: string;
     master_name: string;
     service_id: string;
     service_name: string;
     starts_at: string;
     ends_at: string;
     status: string;
     source_platform: string | null;
     master_username: string | null;
   }

   export interface ClientBookingsResponse {
     upcoming: ClientBookingRead[];
     past: ClientBookingRead[];
   }

   export interface ReviewCreateRequest {
     booking_id: string;
     rating: number;
     text?: string;
   }

   export interface ReviewCreateResponse {
     id: string;
     rating: number;
     text: string | null;
     created_at: string;
   }
   ```

2. **Create public/src/api/client-cabinet.ts**:
   - `requestOTP(phone: string)`: POST /client/auth/request-code with body { phone }. Uses apiRequest. Include `credentials: "include"` in all requests (for cookies).
   - `verifyOTP(phone: string, code: string)`: POST /client/auth/verify-code with body { phone, code }. credentials: "include".
   - `getClientBookings()`: GET /client/bookings. credentials: "include".
   - `createReview(data: ReviewCreateRequest)`: POST /client/reviews. credentials: "include".
   - `cancelBooking(bookingId: string, reason?: string)`: PUT /bookings/${bookingId}/cancel.
   - `rescheduleBooking(bookingId: string, newStartsAt: string)`: PUT /bookings/${bookingId}/reschedule.

   IMPORTANT: All client cabinet API calls MUST include `credentials: "include"` in fetch options so the httpOnly cookie is sent. Update apiRequest calls: `apiRequest<T>(path, { ...options, credentials: "include" })`.

3. **Create public/src/stores/auth.ts** (Zustand store):
   ```typescript
   interface AuthState {
     isAuthenticated: boolean;
     isLoading: boolean;
     phone: string;
     setPhone: (phone: string) => void;
     setAuthenticated: (auth: boolean) => void;
     checkSession: () => Promise<void>;
     logout: () => void;
   }
   ```
   - `checkSession`: try GET /client/bookings with credentials:include. If 200, set isAuthenticated=true. If 401, set false. This is a simple "are we logged in?" check on page load.
   - `logout`: clear the cookie by calling a logout endpoint OR just set isAuthenticated=false and let the cookie expire. For MVP, just set state to false and redirect to /my.

4. **Create public/src/components/LoginForm.tsx**:
   - Two-phase form:
     - **Phase 1 (phone input)**: Input field for Russian phone (+7 XXX XXX-XX-XX format), "Получить код" button.
       - Use same phone formatting as existing InfoStep.tsx (formatPhone/cleanPhone pattern from booking flow)
       - On submit: call requestOTP, if success -> move to phase 2
       - On 404: show error "Клиент не найден. Сначала запишитесь к мастеру."
       - On 429: show cooldown timer
     - **Phase 2 (OTP input)**: 6 separate digit inputs (auto-focus next on input, paste support for full code), countdown timer (60s), "Отправить повторно" button (disabled during countdown).
       - Each digit input: type="tel", maxLength=1, pattern="[0-9]", width 48px, height 56px, text-center, text-2xl, border rounded-xl
       - Auto-submit when 6th digit entered
       - On submit: call verifyOTP
       - On success: call auth store setAuthenticated(true), navigate to /my/bookings
       - On error: show inline error message, shake animation on inputs
       - Countdown timer: useState with setInterval, starts at 60s, shows "Отправить повторно через Xs"
       - When countdown reaches 0: enable "Отправить повторно" button, which calls requestOTP again and resets timer

   Style: Tailwind classes matching existing public/ SPA design. White background, rounded cards, text-primary for labels, primary color for buttons. Mobile-first, max-w-sm mx-auto.

5. **Create public/src/pages/cabinet/LoginPage.tsx**:
   - Simple page wrapper: centered LoginForm with "Личный кабинет" heading
   - If already authenticated (useAuth().isAuthenticated), redirect to /my/bookings via useEffect + navigate
   - Header: "Личный кабинет" (text-2xl font-bold text-text-primary)
   - Subtitle: "Войдите, чтобы видеть свои записи" (text-text-secondary)
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm/public && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
  - Auth store (useAuth) tracks isAuthenticated, phone, provides checkSession and logout
  - API module has all 6 functions (requestOTP, verifyOTP, getClientBookings, createReview, cancelBooking, rescheduleBooking)
  - All API calls include credentials: "include" for cookie auth
  - LoginForm handles two phases (phone input -> OTP input) with 60-second countdown and resend
  - 6-digit OTP input with auto-focus, paste support, auto-submit
  - LoginPage redirects to /my/bookings if already authenticated
  - TypeScript types added to types.ts for all client cabinet data
  </done>
</task>

<task type="auto">
  <name>Task 2: BookingsPage, BookingCard, ReviewForm, and App.tsx routes</name>
  <files>public/src/pages/cabinet/BookingsPage.tsx, public/src/components/BookingCard.tsx, public/src/components/ReviewForm.tsx, public/src/App.tsx</files>
  <action>
1. **Create public/src/components/BookingCard.tsx**:
   - Props: booking (ClientBookingRead), type ("upcoming" | "past"), onReview (callback), onCancel (callback), onReschedule (callback)
   - Card layout (rounded-2xl border border-gray-100 shadow-sm p-4):
     - Top row: master avatar placeholder (circle with initials) + master name (clickable, navigates to /m/{master_username}) + service name
     - Middle: date/time formatted in Russian (dayjs locale ru: "25 марта, Пн · 14:00 - 15:30")
     - Status badge: confirmed="Подтверждена" (green), completed="Завершена" (blue), cancelled_by_client/cancelled_by_master="Отменена" (gray), no_show="Неявка" (red)
     - Bottom actions (flex gap-2):
       - If upcoming + confirmed:
         - "Перенести" button (outline, calls onReschedule) -- for MVP, opens alert "Функция в разработке" since reschedule needs time picker
         - "Отменить" button (outline red, calls onCancel)
       - If past + completed:
         - "Записаться снова" button (primary, navigates to `/m/${master_username}/book?service=${service_id}`)
         - "Оставить отзыв" button (outline, calls onReview) -- only if review not yet left (check via booking status or separate flag; for MVP always show, backend will return 409 if duplicate)

   Import dayjs and configure Russian locale:
   ```typescript
   import dayjs from "dayjs";
   import "dayjs/locale/ru";
   dayjs.locale("ru");
   ```

2. **Create public/src/components/ReviewForm.tsx**:
   - Props: bookingId (string), onSubmit (callback with ReviewCreateResponse), onClose (callback)
   - Star rating: 5 Star icons (lucide-react), clickable, filled=selected, empty=unselected. Size 32px. Hover and click to select rating.
   - Text area: optional, placeholder "Расскажите о визите (необязательно)", max 500 chars, character counter
   - Submit button: "Отправить отзыв" (primary, disabled until rating selected)
   - Loading state during submission
   - Error handling: show inline error on 409/400
   - On success: call onSubmit callback, show brief "Спасибо за отзыв!" message

   Style: Can be rendered inline (expands under the booking card) or as a bottom sheet. For MVP, render inline below the BookingCard when "Оставить отзыв" is clicked.

3. **Create public/src/pages/cabinet/BookingsPage.tsx**:
   - Requires auth: if not authenticated, redirect to /my
   - Fetch bookings: `useQuery({ queryKey: ["client-bookings"], queryFn: getClientBookings })`
   - Header: "Мои записи" (text-2xl font-bold)
   - Two sections with sticky headers:
     - "Предстоящие" (upcoming) -- if empty, show "Нет предстоящих записей"
     - "Прошедшие" (past) -- if empty, show "Пока нет завершённых визитов"
   - Render BookingCard for each booking
   - State management:
     - reviewingBookingId (string | null) -- which booking has review form open
     - cancellingBookingId (string | null) -- which booking is being cancelled (loading state)
   - Cancel action: confirm dialog ("Вы уверены, что хотите отменить запись?"), then call cancelBooking API, invalidate query on success
   - Review action: toggle reviewingBookingId, show ReviewForm inline under the card
   - After successful review: close form, show success toast/message, invalidate bookings query
   - Loading: skeleton cards (3 placeholder cards with animate-pulse)
   - Error: "Не удалось загрузить записи" with retry button

4. **Update public/src/App.tsx**:
   - Import LoginPage and BookingsPage
   - Add routes inside existing Routes block:
     ```tsx
     <Route path="/my" element={<LoginPage />} />
     <Route path="/my/bookings" element={<BookingsPage />} />
     ```
   - These routes sit alongside existing /m/:username routes since basename is now "/" (from Plan 02)

   IMPORTANT: The /my route should show LoginPage if not authenticated, or redirect to /my/bookings if authenticated. The LoginPage component already handles this redirect internally.

Add `dayjs` is already in public/package.json (added in Phase 10). The `dayjs/locale/ru` import is needed. The `lucide-react` is also already installed.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm/public && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
  - BookingsPage shows upcoming and past bookings in two sections
  - BookingCard displays master name (clickable to /m/{username}), service, formatted date/time in Russian, status badge
  - Upcoming cards have cancel and reschedule buttons (reschedule shows "in development" for MVP)
  - Past cards have "Записаться снова" (navigates to /m/{username}/book?service={id}) and "Оставить отзыв"
  - ReviewForm with 5-star rating and optional text, handles 409 duplicate and 400 validation errors
  - Bookings from all masters appear in one list (cross-master by design per API)
  - App.tsx has /my and /my/bookings routes wired
  - pnpm build succeeds with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `cd /Users/yannovak/development/projects/master_crm/public && pnpm build` succeeds
- grep confirms LoginPage imported and routed at /my
- grep confirms BookingsPage imported and routed at /my/bookings
- grep confirms all API calls include credentials: "include"
- grep confirms ReviewForm posts to /client/reviews
</verification>

<success_criteria>
- Client can enter phone number, receive OTP, enter 6-digit code, and see their bookings
- OTP form has countdown timer, resend button, 6 separate digit inputs with auto-focus
- Upcoming bookings show cancel button (functional) and reschedule button (MVP placeholder)
- Past bookings show "Записaться снова" (navigates to booking flow with pre-selected service) and "Оставить отзыв" (inline form)
- Review form has 1-5 star selection, optional text (500 char max), proper error handling
- Bookings from multiple masters visible in single list
- Master name is clickable and navigates to /m/{username}
- Build succeeds with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-client-cabinet/11-03-SUMMARY.md`
</output>
