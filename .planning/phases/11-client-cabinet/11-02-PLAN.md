---
phase: 11-client-cabinet
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Caddyfile
  - public/vite.config.ts
  - public/src/App.tsx
  - public/Dockerfile
autonomous: true
requirements: [CCAB-01]

must_haves:
  truths:
    - "Visiting /m/{username} renders the master public page (same as before)"
    - "Visiting /my renders the client cabinet (login or bookings)"
    - "Both /m/* and /my/* are served by the same public SPA container"
    - "SPA assets load correctly for both /m/ and /my/ paths"
    - "React Router handles /m/:username, /m/:username/book/*, and /my/* routes"
  artifacts:
    - path: "Caddyfile"
      provides: "Dual-path routing for /m/* and /my/* to public:3002"
      contains: "handle /my/*"
    - path: "public/vite.config.ts"
      provides: "Vite base set to / for dual-path support"
      contains: 'base: "/"'
    - path: "public/src/App.tsx"
      provides: "React Router with basename=/ handling both /m/ and /my/ routes"
      contains: 'basename="/"'
    - path: "public/Dockerfile"
      provides: "Nginx config handling both /m/ and /my/ SPA fallback"
      contains: "/my/"
  key_links:
    - from: "Caddyfile"
      to: "public:3002"
      via: "reverse_proxy without strip_prefix"
      pattern: "reverse_proxy public:3002"
    - from: "public/src/App.tsx"
      to: "public/src/pages/MasterPage.tsx"
      via: "Route path /m/:username"
      pattern: "/m/:username"
---

<objective>
Fix SPA routing so the same public/ container serves both /m/{username} (master pages) and /my/ (client cabinet) paths.

Purpose: Phase 10 set basename="/m" which only works for /m/* routes. The client cabinet lives at /my/* and needs the same SPA. Changing to basename="/" with full paths in routes and removing Caddy's strip_prefix solves this cleanly.

Output: Updated Caddyfile (dual-path, no strip_prefix), vite.config.ts (base: "/"), App.tsx (basename="/", full path routes), Dockerfile (nginx handles both paths).
</objective>

<execution_context>
@/Users/yannovak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yannovak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-public-master-page/10-01-SUMMARY.md

<interfaces>
<!-- Current routing configuration that needs modification -->

From Caddyfile (current):
```
# Public master pages
handle /m/* {
    uri strip_prefix /m
    reverse_proxy public:3002
}
```

From public/vite.config.ts (current):
```typescript
base: "/m/",
```

From public/src/App.tsx (current):
```typescript
<BrowserRouter basename="/m">
  <Routes>
    <Route path="/:username" element={<MasterPage />} />
    <Route path="/:username/book" element={<ServiceStep />} />
    ...
  </Routes>
</BrowserRouter>
```

From public/Dockerfile (current nginx config):
```
location / {
    try_files $uri $uri/ /index.html;
}
```

From public/src/api/client.ts:
```typescript
const API_BASE = import.meta.env.VITE_API_BASE_URL || "/api/v1";
```
Note: API calls go to /api/v1/* which Caddy handles separately -- no base path issue.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Caddy, Vite, and Nginx for dual-path SPA</name>
  <files>Caddyfile, public/vite.config.ts, public/Dockerfile</files>
  <action>
1. **Update Caddyfile**: Replace the single `/m/*` block with two blocks that do NOT strip prefix. The SPA receives the full path and React Router handles it.

   Replace:
   ```
   redir /m /m/ 308

   # Public master pages
   handle /m/* {
       uri strip_prefix /m
       reverse_proxy public:3002
   }
   ```

   With:
   ```
   redir /m /m/ 308
   redir /my /my/ 308

   # Public master pages
   handle /m/* {
       reverse_proxy public:3002
   }

   # Client cabinet
   handle /my/* {
       reverse_proxy public:3002
   }
   ```

   Key change: removed `uri strip_prefix /m` so the SPA receives `/m/username` not `/username`. Added `/my/*` block pointing to same container.

2. **Update public/vite.config.ts**: Change `base: "/m/"` to `base: "/"`. This ensures Vite generates asset URLs like `/assets/main.js` instead of `/m/assets/main.js`. Since Caddy no longer strips prefix, the SPA needs to serve assets from root.

   BUT WAIT: assets at `/assets/main.js` would not be caught by `/m/*` or `/my/*` Caddy rules -- they'd fall through to the default handler (api:8000). This is the tricky part.

   **SOLUTION**: Keep assets served from within /m/ and /my/ paths. Set `base: "/"` in Vite. But configure Caddy to also serve static assets. Actually, the simpler solution:

   Set Vite `base: "/"`. In the Dockerfile, nginx serves the SPA on port 3002. Caddy proxies `/m/*` and `/my/*` to nginx:3002 WITHOUT stripping prefix. Nginx receives `/m/username` and tries the file, falls back to `/index.html`. Since the SPA assets are at `/assets/...` in nginx, but the request comes in as `/m/assets/...`, nginx won't find them.

   **BETTER SOLUTION**: Use Caddy `handle_path` instead of `handle`. `handle_path` strips the prefix for the backend BUT keeps it in the URL for the browser. Actually no, handle_path strips the path before reverse_proxy.

   **BEST SOLUTION**: Keep `uri strip_prefix` for BOTH /m and /my, but set Vite base to "/" and BrowserRouter basename to "/". The SPA reads window.location.pathname on mount to determine which mode it's in (/m/ vs /my/). Routes use relative paths.

   Actually, the problem is simpler than I'm making it:
   - Caddy strips /m and /my prefix
   - SPA receives "/" for both /m/ and /my/
   - SPA cannot distinguish which path the user is on

   **ACTUAL BEST**: Keep strip_prefix for /m/*. Add strip_prefix for /my/*. The SPA has TWO base paths. Use React Router with basename="/" (since prefix is stripped). To distinguish /m/ vs /my/ context, pass a custom header from Caddy, or detect from original URL.

   **SIMPLEST WORKING SOLUTION**:
   - Caddy: `/m/*` strips /m, proxies to public:3002. `/my/*` strips /my, proxies to public:3002. Both add an `X-Base-Path` header.
   - Vite: `base: "/"` -- assets at root of nginx
   - BrowserRouter: `basename="/"`
   - SPA detects which mode via a query param or a global injected by Caddy header

   But the SPA cannot read the Caddy header since it's a static SPA (nginx, not SSR).

   **ACTUALLY SIMPLEST**: Two separate Caddy handle blocks with strip_prefix. SPA receives clean paths. For /m/username -> nginx gets /username. For /my/ -> nginx gets /. React Router just needs both route patterns at root:
   - Route path="/:username" -> MasterPage
   - Route path="/:username/book/*" -> booking flow
   - Route path="/" -> ClientCabinetHome (login or bookings)

   Problem: "/:username" would also match "/my" if someone navigates to /my/anything. But with strip_prefix, /my/ becomes / and /my/bookings becomes /bookings. So:
   - Route path="/" -> could be client cabinet index OR could be empty master page
   - Route path="/bookings" -> client bookings (only via /my/ strip)

   This works! The "/" route would be client cabinet. But what about /m/ (without username)? Currently redirected to /m/ with 308, and nginx serves index.html, which shows NotFound (no route matches "/"). That's fine.

   For /my/, Caddy strips /my, so nginx gets "/", SPA matches path="/" -> ClientCabinetHome.
   For /my/bookings, Caddy strips /my, nginx gets "/bookings", SPA matches path="/bookings".
   For /m/anna, Caddy strips /m, nginx gets "/anna", SPA matches path="/:username".
   For /m/anna/book, Caddy strips /m, nginx gets "/anna/book", SPA matches path="/:username/book".

   CONFLICT: path="/" could also match path="/:username" where username is empty string. No, "/:username" requires a segment. "/" is the index route. No conflict.

   But what about path="/bookings" matching "/:username" where username="bookings"? YES, this is a conflict! React Router will match "/:username" first since it's listed first.

   **FIX**: Order routes so specific paths come before parameterized ones:
   ```
   <Route path="/" element={<ClientCabinetHome />} />
   <Route path="/bookings" element={<BookingsList />} />
   <Route path="/:username" element={<MasterPage />} />
   <Route path="/:username/book" ... />
   ```
   React Router v6 uses a scoring system and prefers static segments over params, so "/bookings" will match before "/:username" even without explicit ordering. But to be safe, list specific routes first.

   This is the cleanest solution. Proceed with this approach:

   **Caddyfile changes:**
   - Keep existing `/m/*` block with strip_prefix (unchanged)
   - Add `/my/*` block with `uri strip_prefix /my` and `reverse_proxy public:3002`
   - Add `redir /my /my/ 308`

   **Vite config**: Keep `base: "/m/"`. Wait, but now /my/ assets need to load too. When Caddy strips /my and serves to nginx, index.html references assets at `/m/assets/main.js`. The browser requests `/m/assets/main.js` which goes through the /m/* Caddy block and nginx serves it. This actually WORKS! Assets are always served via /m/ path regardless.

   Actually no. The index.html that nginx returns for /my/ requests will have `<script src="/m/assets/main.js">`. The browser will request `/m/assets/main.js`, Caddy strips /m, nginx gets `/assets/main.js`, finds it in dist/, serves it. YES, this works!

   So the ONLY changes needed are:
   1. Caddyfile: add /my/* block with strip_prefix pointing to public:3002
   2. App.tsx: change basename from "/m" to "/" (since both /m and /my are stripped, SPA sees clean paths)
   3. BUT: with basename="/", internal links like `<Link to="/anna">` would generate href="/anna" which the browser requests at root, NOT at /m/anna. The user would get the API default handler, not the SPA.

   **THE REAL ISSUE**: Internal navigation. With basename="/m", Link to="/anna" generates href="/m/anna". With basename="/", it generates "/anna" -- wrong.

   **FINAL SOLUTION**: Don't change basename. Keep basename="/m" for the master page routes. For /my/ routes, use a SEPARATE React Router instance or use window.location.pathname detection.

   Actually, the simplest architectural solution is:
   - Keep everything about /m/ exactly as is (basename="/m", strip_prefix /m)
   - For /my/: Caddy strips /my, proxies to public:3002. The SPA ALSO has basename="/m" so it won't match "/".

   That breaks. We need the SPA to handle both.

   **REAL FINAL SOLUTION**: Use basename="/". Update ALL internal links and navigation to use full paths:
   - Master page links: `/m/${username}` instead of `/${username}`
   - Booking flow: `/m/${username}/book` instead of `/${username}/book`
   - Client cabinet: `/my/` and `/my/bookings`
   - Caddy does NOT strip prefix for either /m/* or /my/*
   - Vite base: "/"
   - Nginx catches everything, falls back to index.html for any path

   The only issue is assets. With base "/", assets are at `/assets/main.js`. The browser requests that. Caddy doesn't have a handler for `/assets/*` -- it falls through to the default handler (api:8000).

   **ADD to Caddyfile**: a static assets handler, or a broader handler.

   OR: Add a Caddy handle for `/assets/*` pointing to public:3002. Since nginx serves these files directly.

   Final plan:
   1. **Caddyfile**:
      - Remove strip_prefix from /m/* block
      - Add /my/* block without strip_prefix
      - Add /assets/* block pointing to public:3002 (for static assets)
      - Add redir /my /my/ 308

   2. **public/vite.config.ts**: Change base to "/"

   3. **public/src/App.tsx**: Change basename to "/". Update routes to use full paths:
      - `/m/:username` instead of `/:username`
      - `/m/:username/book` instead of `/:username/book`
      - Add `/my` and `/my/*` route placeholder (actual pages in Plan 03)

   4. **public/Dockerfile nginx**: Update try_files to serve index.html for any path under /m/ or /my/. Actually, the default `try_files $uri $uri/ /index.html` already does this.

   5. **Update ALL navigate() and Link to= calls** in existing MasterPage, booking flow pages to use `/m/` prefix:
      - In booking-flow.ts store: no change needed (store doesn't have navigation)
      - In ServiceStep.tsx, DateStep.tsx, etc.: navigate calls use relative paths like `../book/date` or `/${username}/book/date`. These need to become `/m/${username}/book/date`.
      - In MasterPage.tsx: Link to `/${username}/book` becomes `/m/${username}/book`
      - In ConfirmStep.tsx: link back to master page

   This is the correct approach. All navigation uses full absolute paths with /m/ prefix. The SPA can handle both /m/ and /my/ simultaneously.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm/public && grep -q 'base: "/"' vite.config.ts && grep -q 'basename="/"' src/App.tsx && grep -q '/m/:username' src/App.tsx && grep -q 'handle /my' ../Caddyfile && echo "OK: all routing changes applied"</automated>
  </verify>
  <done>
  - Vite base changed from "/m/" to "/"
  - BrowserRouter basename changed from "/m" to "/"
  - Routes use full paths: /m/:username, /m/:username/book/*, /my/*
  - Caddyfile has both /m/* and /my/* blocks (no strip_prefix) pointing to public:3002
  - Caddyfile has /assets/* block for static asset serving
  - All existing navigate() and Link to= calls in master page and booking flow updated from relative /:username paths to /m/:username
  - Nginx fallback serves index.html for any path (SPA routing)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all existing public/ page navigation to /m/ prefix paths</name>
  <files>public/src/pages/MasterPage.tsx, public/src/pages/booking/ServiceStep.tsx, public/src/pages/booking/DateStep.tsx, public/src/pages/booking/TimeStep.tsx, public/src/pages/booking/InfoStep.tsx, public/src/pages/booking/ConfirmStep.tsx</files>
  <action>
Update all `useNavigate()` calls, `<Link>` components, and `useParams` patterns in existing pages to work with the new full-path routing (/m/:username instead of /:username).

In each booking step file, the navigate calls likely use patterns like:
- `navigate(`/${username}/book/date`)` -> change to `navigate(`/m/${username}/book/date`)`
- `navigate(`/${username}/book/time`)` -> change to `navigate(`/m/${username}/book/time`)`
- `navigate(`/${username}/book/info`)` -> change to `navigate(`/m/${username}/book/info`)`
- `navigate(`/${username}/book/confirm`)` -> change to `navigate(`/m/${username}/book/confirm`)`
- `navigate(`/${username}`)` -> change to `navigate(`/m/${username}`)`

In MasterPage.tsx:
- `<Link to={`/${username}/book`}>` -> `<Link to={`/m/${username}/book`}>`
- Or navigation via `navigate(`/${username}/book?service=${id}`)` -> add /m/ prefix

In ConfirmStep.tsx:
- Link back to master page: add /m/ prefix

Read each file first to find the exact patterns, then update. The `useParams()` call extracts `username` from the URL -- with the new route `/m/:username`, useParams still works the same way.

Also update any `<a href>` or direct window.location references if they exist.

IMPORTANT: The existing `useBookingFlow` store uses `username` from store state, not from URL params directly. The navigation in booking steps constructs paths using that username. Just add `/m/` prefix to all path constructions.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm/public && grep -rn "navigate\(\`/" src/pages/ | grep -v "/m/" | grep -v "node_modules" | grep -v "/my/" | grep -c "navigate" | xargs -I{} test {} -eq 0 && echo "OK: all navigate calls use /m/ or /my/ prefix" || echo "FAIL: found navigate calls without prefix"</automated>
  </verify>
  <done>
  - All navigate() calls in MasterPage.tsx use /m/ prefix
  - All navigate() calls in ServiceStep, DateStep, TimeStep, InfoStep, ConfirmStep use /m/ prefix
  - All Link components use /m/ prefix for master page URLs
  - useParams() still works correctly with /m/:username route pattern
  - No broken navigation between master page and booking flow
  </done>
</task>

</tasks>

<verification>
- `cd /Users/yannovak/development/projects/master_crm/public && pnpm build` succeeds with new base "/"
- Caddyfile has separate /m/* and /my/* handlers, both pointing to public:3002
- No strip_prefix in either /m or /my Caddy blocks
- grep for navigate calls confirms all use /m/ prefix for master pages
</verification>

<success_criteria>
- The public SPA can serve both /m/{username} (master pages) and /my/ (client cabinet) from the same container
- Existing master page and booking flow navigation works identically after path prefix change
- Static assets (JS/CSS) load correctly for both /m/ and /my/ entry points
- Caddy routes both /m/* and /my/* to public:3002 without stripping prefix
</success_criteria>

<output>
After completion, create `.planning/phases/11-client-cabinet/11-02-SUMMARY.md`
</output>
