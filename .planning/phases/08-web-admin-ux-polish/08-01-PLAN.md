---
phase: 08-web-admin-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/StatusTag.tsx
  - web/src/layouts/AdminLayout.tsx
  - web/src/App.tsx
autonomous: true
requirements: [WCRT-03, WCRT-04, WAUX-01, WAUX-02, WAUX-06, WAUX-09, WAUX-10]

must_haves:
  truths:
    - "StatusTag displays plain Russian text, not Unicode escapes"
    - "AdminLayout sidebar menu labels are plain Russian text"
    - "Magic link callback navigates via React Router, not window.location"
    - "Dark mode toggle shows SunOutlined when dark, MoonOutlined when light, with aria-label"
    - "Header displays breadcrumb showing current page name and master's business_name"
    - "Sidebar collapsed state persists across page reloads via localStorage"
    - "Each admin page has a unique document.title for browser tab identification"
    - "QueryClient has staleTime of 300000ms (5 minutes) as default"
  artifacts:
    - path: "web/src/components/StatusTag.tsx"
      provides: "Booking and payment status tags with plain UTF-8 Russian labels"
      contains: "Подтверждена"
    - path: "web/src/layouts/AdminLayout.tsx"
      provides: "Admin shell with breadcrumb, profile display, persisted sidebar, a11y toggle"
      contains: "localStorage"
    - path: "web/src/App.tsx"
      provides: "App root with QueryClient staleTime and React Router magic link"
      contains: "staleTime"
  key_links:
    - from: "web/src/App.tsx"
      to: "react-router-dom"
      via: "useNavigate in MagicLinkCallback"
      pattern: "navigate\\("
    - from: "web/src/layouts/AdminLayout.tsx"
      to: "localStorage"
      via: "sidebar collapsed state persistence"
      pattern: "localStorage\\.(get|set)Item.*collapsed"
---

<objective>
Fix global UI issues across the web admin panel: replace Unicode escape strings with plain Russian text, fix magic link navigation, improve AdminLayout header with breadcrumb and profile display, add dark mode toggle accessibility, persist sidebar state, set page titles per route, and configure QueryClient caching.

Purpose: These 7 requirements address string encoding bugs, navigation correctness, and foundational UX improvements that affect every page of the admin panel.
Output: Three corrected files (StatusTag.tsx, AdminLayout.tsx, App.tsx) with proper Russian strings, accessible dark mode toggle, breadcrumb navigation, sidebar persistence, page titles, and smart caching.
</objective>

<execution_context>
@/Users/yannovak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yannovak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From web/src/stores/auth.ts:
```typescript
export interface MasterProfile {
  id: string;
  email: string | null;
  phone: string | null;
  name: string;
  business_name: string | null;
  timezone: string;
  is_active: boolean;
  created_at: string;
}
// useAuth store has: profile: MasterProfile | null
```

From web/src/stores/theme.ts:
```typescript
interface ThemeState {
  isDark: boolean;
  toggle: () => void;
}
export const useThemeStore = create<ThemeState>(...)
```

From web/src/api/client.ts:
```typescript
export async function apiRequest<T>(path: string, options?: RequestInit): Promise<T>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix StatusTag UTF-8 strings + App.tsx magic link navigation + QueryClient staleTime</name>
  <files>web/src/components/StatusTag.tsx, web/src/App.tsx</files>
  <read_first>web/src/components/StatusTag.tsx, web/src/App.tsx</read_first>
  <action>
**StatusTag.tsx (WCRT-03):**
Replace all Unicode escape sequences with plain UTF-8 Russian strings in both bookingStatusMap and paymentStatusMap:
- `"\u041f\u043e\u0434\u0442\u0432\u0435\u0440\u0436\u0434\u0435\u043d\u0430"` -> `"Подтверждена"`
- `"\u041e\u0436\u0438\u0434\u0430\u043d\u0438\u0435"` -> `"Ожидание"`
- `"\u0417\u0430\u0432\u0435\u0440\u0448\u0435\u043d\u0430"` -> `"Завершена"`
- `"\u041e\u0442\u043c\u0435\u043d\u0435\u043d\u0430"` -> `"Отменена"`
- `"\u041e\u0442\u043c\u0435\u043d\u0430 \u043a\u043b\u0438\u0435\u043d\u0442\u043e\u043c"` -> `"Отмена клиентом"`
- `"\u041d\u0435\u044f\u0432\u043a\u0430"` -> `"Неявка"`
- paymentStatusMap: `"\u041e\u043f\u043b\u0430\u0447\u0435\u043d"` -> `"Оплачен"`, `"\u0412\u043e\u0437\u0432\u0440\u0430\u0442"` -> `"Возврат"`, `"\u041e\u0442\u043c\u0435\u043d\u0451\u043d"` -> `"Отменён"`

**App.tsx (WCRT-04):**
In MagicLinkCallback component:
1. Add `import { useNavigate } from "react-router-dom"` (already imported at top level but MagicLinkCallback needs it)
2. Add `const navigate = useNavigate()` inside MagicLinkCallback
3. Replace `window.location.href = "/admin/calendar"` with `navigate("/calendar")`
4. Replace `window.location.href = "/admin/login"` with `navigate("/login")`

**App.tsx (WAUX-10):**
Change the QueryClient instantiation from `new QueryClient()` to:
```typescript
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000, // 5 minutes
      retry: 1,
    },
  },
});
```
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm/web && grep -c '\\\\u0' src/components/StatusTag.tsx | grep -q '^0$' && grep -q 'Подтверждена' src/components/StatusTag.tsx && grep -q 'navigate(' src/App.tsx && grep -q 'staleTime' src/App.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- StatusTag.tsx contains zero Unicode escape sequences (no `\u0` patterns)
- StatusTag.tsx contains plain Russian: "Подтверждена", "Ожидание", "Завершена", "Отменена", "Неявка", "Оплачен"
- App.tsx MagicLinkCallback uses navigate("/calendar") not window.location.href
- App.tsx QueryClient has staleTime: 300000 (5 * 60 * 1000)
  </acceptance_criteria>
  <done>StatusTag shows plain Russian text. Magic link uses React Router navigation. QueryClient has 5-minute default staleTime.</done>
</task>

<task type="auto">
  <name>Task 2: AdminLayout -- dark mode a11y, breadcrumb, profile, sidebar persist, page titles</name>
  <files>web/src/layouts/AdminLayout.tsx</files>
  <read_first>web/src/layouts/AdminLayout.tsx, web/src/stores/auth.ts</read_first>
  <action>
Rewrite AdminLayout.tsx with these changes:

**Menu labels (WCRT-03 continued):**
Replace Unicode escapes in menuItems array with plain Russian:
- `"\u041a\u0430\u043b\u0435\u043d\u0434\u0430\u0440\u044c"` -> `"Календарь"`
- `"\u041a\u043b\u0438\u0435\u043d\u0442\u044b"` -> `"Клиенты"`
- `"\u0423\u0441\u043b\u0443\u0433\u0438"` -> `"Услуги"`
- `"\u041f\u043b\u0430\u0442\u0435\u0436\u0438"` -> `"Платежи"`
- `"\u041d\u0430\u0441\u0442\u0440\u043e\u0439\u043a\u0438"` -> `"Настройки"`

**Dark mode toggle (WAUX-01):**
1. Import `SunOutlined` and `MoonOutlined` from `@ant-design/icons`
2. Replace both `BulbOutlined` children on the Switch:
   - `checkedChildren={<MoonOutlined />}` (dark mode is ON)
   - `unCheckedChildren={<SunOutlined />}` (light mode is ON)
3. Add `aria-label={isDark ? "Переключить на светлую тему" : "Переключить на тёмную тему"}` to the Switch

**Breadcrumb + profile display (WAUX-02):**
1. Import `Breadcrumb, Space, Typography` from antd
2. Import `useAuth` from stores
3. Get profile: `const profile = useAuth((s) => s.profile)`
4. Create a `PAGE_TITLES` map: `{ "/calendar": "Календарь", "/clients": "Клиенты", "/services": "Услуги", "/payments": "Платежи", "/settings": "Настройки" }`
5. Derive current page title from `location.pathname` using PAGE_TITLES (default to pathname for /clients/:id -> "Клиенты")
6. In the Header, change from `justifyContent: "flex-end"` to `justifyContent: "space-between"`
7. Add left side: `<Space>` with `<Breadcrumb items={[{ title: "MasterCRM" }, { title: currentPageTitle }]} />`
8. Add right side (existing toggle + logout): also add `<Typography.Text type="secondary">{profile?.business_name || profile?.name || ""}</Typography.Text>` before the toggle

**Sidebar collapse persistence (WAUX-06):**
1. Replace `useState(false)` with `useState(() => { try { return localStorage.getItem("admin_sidebar_collapsed") === "true"; } catch { return false; } })`
2. Replace `onCollapse={setCollapsed}` with `onCollapse={(val) => { setCollapsed(val); try { localStorage.setItem("admin_sidebar_collapsed", String(val)); } catch {} }}`

**Page titles (WAUX-09):**
1. Add a `useEffect` that sets `document.title` based on current route:
```typescript
useEffect(() => {
  const base = location.pathname.startsWith("/clients/") ? "/clients" : location.pathname;
  const title = PAGE_TITLES[base] || "MasterCRM";
  document.title = `${title} — MasterCRM`;
}, [location.pathname]);
```
This sets titles like "Календарь — MasterCRM", "Клиенты — MasterCRM", etc.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm/web && grep -q 'Календарь' src/layouts/AdminLayout.tsx && grep -q 'SunOutlined' src/layouts/AdminLayout.tsx && grep -q 'MoonOutlined' src/layouts/AdminLayout.tsx && grep -q 'aria-label' src/layouts/AdminLayout.tsx && grep -q 'Breadcrumb' src/layouts/AdminLayout.tsx && grep -q 'business_name' src/layouts/AdminLayout.tsx && grep -q 'admin_sidebar_collapsed' src/layouts/AdminLayout.tsx && grep -q 'document.title' src/layouts/AdminLayout.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- Menu labels are plain Russian ("Календарь", "Клиенты", "Услуги", "Платежи", "Настройки") with no Unicode escapes
- Dark mode Switch uses SunOutlined (unchecked) and MoonOutlined (checked) with aria-label attribute
- Header left side has Breadcrumb showing "MasterCRM > {page name}"
- Header right side shows profile.business_name or profile.name before toggle
- Sidebar collapsed state reads from and writes to localStorage key "admin_sidebar_collapsed"
- document.title updates on route change to "{PageName} — MasterCRM"
  </acceptance_criteria>
  <done>AdminLayout has accessible dark mode toggle, breadcrumb navigation, profile display, persistent sidebar state, and per-route page titles.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/yannovak/development/projects/master_crm/web
# Verify no Unicode escapes remain in StatusTag or AdminLayout
grep -c '\\u0' src/components/StatusTag.tsx src/layouts/AdminLayout.tsx
# Should show 0 for both files

# Verify build succeeds
npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
1. All Russian text in StatusTag.tsx and AdminLayout.tsx is plain UTF-8 (zero \u escape sequences)
2. Magic link redirect uses navigate() from react-router-dom
3. Dark mode Switch has SunOutlined/MoonOutlined icons with aria-label
4. Header shows breadcrumb on left, profile name + controls on right
5. Sidebar collapse state survives page reload
6. Browser tab shows route-specific title
7. QueryClient default staleTime is 5 minutes
8. TypeScript compilation passes (npx tsc --noEmit)
</success_criteria>

<output>
After completion, create `.planning/phases/08-web-admin-ux-polish/08-01-SUMMARY.md`
</output>
