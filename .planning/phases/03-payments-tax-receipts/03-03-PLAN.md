---
phase: 03-payments-tax-receipts
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - frontend/src/api/payments.ts
  - frontend/src/api/master-settings.ts
  - frontend/src/components/PaymentSheet.tsx
  - frontend/src/components/ReceiptDataCard.tsx
  - frontend/src/components/RobokassaWizard.tsx
  - frontend/src/pages/master/PaymentHistory.tsx
  - frontend/src/pages/master/Bookings.tsx
  - frontend/src/pages/master/Settings.tsx
  - frontend/src/App.tsx
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, TAX-01, TAX-02, TAX-03]

must_haves:
  truths:
    - "Master can tap Zavershit on a booking and see a bottom sheet with 3 payment options"
    - "Selecting manual income entry records the payment and shows receipt data if fiscalization is manual"
    - "Selecting Robokassa SBP sends a payment link to the client via bot"
    - "Selecting show requisites displays QR code + card/phone number for client"
    - "Master can view payment history list with status badges and date filters"
    - "Master can configure payment requisites (card, phone, bank) in Settings"
    - "Master can connect Robokassa via step-by-step wizard in Settings"
    - "Master can select fiscalization level in Settings with one-time grey warning"
  artifacts:
    - path: "frontend/src/components/PaymentSheet.tsx"
      provides: "Payment flow bottom sheet with 3 options"
      min_lines: 100
    - path: "frontend/src/components/ReceiptDataCard.tsx"
      provides: "Copyable receipt data for manual fiscalization"
      min_lines: 30
    - path: "frontend/src/components/RobokassaWizard.tsx"
      provides: "Step-by-step Robokassa connection wizard"
      min_lines: 80
    - path: "frontend/src/pages/master/PaymentHistory.tsx"
      provides: "Payment list with filters and status badges"
      min_lines: 80
    - path: "frontend/src/api/payments.ts"
      provides: "TanStack Query hooks for payment endpoints"
      exports: ["useCreateManualPayment", "useCreateRobokassaPayment", "useCreateRequisitesPayment", "usePaymentHistory"]
  key_links:
    - from: "frontend/src/components/PaymentSheet.tsx"
      to: "frontend/src/api/payments.ts"
      via: "mutation hooks for payment creation"
      pattern: "useCreateManualPayment|useCreateRobokassaPayment|useCreateRequisitesPayment"
    - from: "frontend/src/pages/master/Bookings.tsx"
      to: "frontend/src/components/PaymentSheet.tsx"
      via: "Zavershit button opens PaymentSheet"
      pattern: "PaymentSheet"
    - from: "frontend/src/pages/master/Settings.tsx"
      to: "frontend/src/components/RobokassaWizard.tsx"
      via: "Robokassa section renders wizard"
      pattern: "RobokassaWizard"
    - from: "frontend/src/pages/master/Settings.tsx"
      to: "frontend/src/api/master-settings.ts"
      via: "payment settings hooks"
      pattern: "usePaymentSettings|useUpdatePaymentSettings"
---

<objective>
Build the frontend payment UI: payment flow bottom sheet (3 options), payment history screen, receipt data card, Robokassa connection wizard, and Settings payment configuration sections. Wire everything to the backend API endpoints created in Plans 01-02.

Purpose: This is the user-facing layer that enables masters to actually collect payments and configure payment settings. Without the UI, the backend payment infrastructure is inaccessible.

Output: 4 new components, 1 new page, 1 new API hook file, 2 extended files (Settings, Bookings), route registration.
</objective>

<execution_context>
@/Users/yannovak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yannovak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-payments-tax-receipts/03-CONTEXT.md
@.planning/phases/03-payments-tax-receipts/03-RESEARCH.md
@.planning/phases/02-booking-engine-telegram/02-UI-SPEC.md
@.planning/phases/03-payments-tax-receipts/03-01-SUMMARY.md
@.planning/phases/03-payments-tax-receipts/03-02-SUMMARY.md

<interfaces>
<!-- Frontend patterns and API contracts -->

From frontend/src/api/master-settings.ts (CURRENT - to be extended):
```typescript
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { masterApiRequest } from "../stores/master-auth.ts";

export interface MasterSettings {
  buffer_minutes: number;
  cancellation_deadline_hours: number;
  slot_interval_minutes: number;
}
export function useMasterSettings(): UseQueryResult<MasterSettings>
export function useUpdateSettings(): UseMutationResult
```

From frontend/src/stores/master-auth.ts:
```typescript
export function masterApiRequest<T>(path: string, options?: RequestInit): Promise<T>
// Attaches Bearer token from Zustand store
```

From frontend/src/components/ui/ (existing shared components):
```
Button.tsx    -- Primary/Secondary variants, h-44px, rounded-12px
Card.tsx      -- rounded-14px, border gray-200, padding 16px, shadow
Badge.tsx     -- Variants: confirmed (emerald), pending (amber), cancelled (red)
Toast.tsx     -- Fixed bottom, auto-dismiss 3s
```

From frontend/src/components/ConfirmDialog.tsx:
```typescript
// Bottom sheet pattern with slide-up animation, destructive/default variants
export function ConfirmDialog({ open, title, message, confirmLabel, onConfirm, onCancel, destructive })
```

From frontend/src/pages/master/Bookings.tsx:
```typescript
// Booking list with date/status filters, cancel action
// Has BookingCard rendering, needs "Zavershit" button addition
```

From frontend/src/pages/master/Settings.tsx:
```typescript
// Buffer, deadline, interval settings with pill selectors
// Needs payment config sections appended
```

From frontend/src/App.tsx:
```typescript
// Master routes under /master with MasterLayout + BottomTabBar
// Needs /master/payments route added
```

Backend API endpoints (from Plan 02):
```
POST /api/v1/payments/manual        -> PaymentRead
POST /api/v1/payments/robokassa     -> PaymentRead (with payment_url)
POST /api/v1/payments/requisites    -> PaymentRead + PaymentRequisites
POST /api/v1/payments/{id}/confirm  -> PaymentRead
POST /api/v1/payments/{id}/cancel   -> PaymentRead
GET  /api/v1/payments/history       -> PaymentListResponse
GET  /api/v1/payments/{id}/receipt-data -> ManualReceiptData
GET  /api/v1/settings/payment       -> PaymentSettings
PUT  /api/v1/settings/payment       -> PaymentSettings
POST /api/v1/settings/payment/robokassa    -> PaymentSettings
DELETE /api/v1/settings/payment/robokassa  -> PaymentSettings
POST /api/v1/settings/payment/grey-warning-seen -> 204
```

Design tokens from 02-UI-SPEC.md:
```
Accent: #6C5CE7, Text primary: #1A1A2E, Text secondary: #6B7280
Border: #E5E7EB, Success: #10B981, Destructive: #EF4444
Body: 14px/400, Title: 16px/600, Heading: 20px/600, Caption: 12px/400
Spacing: xs=4, sm=8, md=16, lg=24, xl=32
Touch targets: min 44px height
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Payment API hooks, PaymentSheet, ReceiptDataCard, and PaymentHistory page</name>
  <files>
    frontend/src/api/payments.ts
    frontend/src/components/PaymentSheet.tsx
    frontend/src/components/ReceiptDataCard.tsx
    frontend/src/pages/master/PaymentHistory.tsx
    frontend/src/pages/master/Bookings.tsx
    frontend/src/App.tsx
  </files>
  <read_first>
    frontend/src/api/master-bookings.ts
    frontend/src/api/master-settings.ts
    frontend/src/stores/master-auth.ts
    frontend/src/components/ConfirmDialog.tsx
    frontend/src/components/ui/Button.tsx
    frontend/src/components/ui/Card.tsx
    frontend/src/components/ui/Badge.tsx
    frontend/src/components/ui/Toast.tsx
    frontend/src/pages/master/Bookings.tsx
    frontend/src/App.tsx
    frontend/src/index.css
  </read_first>
  <action>
    1. **Create payments API hooks** (frontend/src/api/payments.ts):
       TypeScript interfaces matching backend schemas:
       ```typescript
       interface PaymentRead {
         id: string; booking_id: string; amount: number; status: string;
         payment_method: string | null; receipt_status: string;
         fiscalization_level: string | null; paid_at: string | null;
         created_at: string; service_name: string; client_name: string;
         payment_url: string | null;
       }
       interface PaymentRequisites {
         card_number: string | null; sbp_phone: string | null;
         bank_name: string | null; qr_code_base64: string;
       }
       interface ManualReceiptData {
         amount_display: string; service_name: string;
         client_name: string; date: string;
       }
       interface PaymentListResponse { items: PaymentRead[]; total: number; }
       ```
       Hooks using masterApiRequest:
       - `useCreateManualPayment()` -- POST /payments/manual, mutation
       - `useCreateRobokassaPayment()` -- POST /payments/robokassa, mutation
       - `useCreateRequisitesPayment()` -- POST /payments/requisites, mutation
       - `useConfirmPayment()` -- POST /payments/{id}/confirm, mutation
       - `useCancelPayment()` -- POST /payments/{id}/cancel, mutation
       - `usePaymentHistory(filters)` -- GET /payments/history with query params, useQuery
       - `useReceiptData(paymentId)` -- GET /payments/{id}/receipt-data, useQuery (enabled only when paymentId provided)
       All mutations invalidate ["payments", "history"] query key on success.

    2. **Create PaymentSheet** (frontend/src/components/PaymentSheet.tsx, ~200 lines):
       Bottom sheet (same pattern as ConfirmDialog -- slide-up from bottom, backdrop blur) with 3 states:

       **State 1: Option Selection** (initial)
       - Heading: "Sposob oplaty" (20px semibold)
       - Three tappable cards (min h-44px, md padding):
         a. "Vnesti dokhod vruchnuyu" -- icon: banknotes, subtitle: "Nalichnye, karta, perevod"
         b. "Otpravit ssylku klientu" -- icon: link, subtitle: "SBP cherez Robokassu" (disabled + greyed if !hasRobokassa)
         c. "Pokazat rekvizity + QR" -- icon: qr-code, subtitle: "Klient perevedyot sam" (disabled if no card_number and no sbp_phone)
       - Each card: Card component, 16px padding, accent border on tap

       **State 2a: Manual Payment Form** (after selecting option 1)
       - Heading: "Otmetit oplatu"
       - Payment method pill selector: Nalichnye | Karta | Perevod | SBP
       - Fiscalization override: pill selector showing current default, allow override (Bez chekov | Ruchnoy | Avto)
         - If "Bez chekov" selected AND !hasSeenGreyWarning: show one-time warning in amber Card: "Rekomenduem vystavlyat cheki dlya zashchity ot shtrafov" + "Ponyatno" button that calls POST /settings/payment/grey-warning-seen
       - "Oplacheno" primary CTA button. On success: if fiscalization=manual, show ReceiptDataCard. Else show success toast and close.

       **State 2b: Robokassa Result** (after selecting option 2)
       - Show spinner during API call
       - On success: show success message "Ssylka otpravlena klientu" + checkmark icon
       - Show payment_url as fallback "Ili skopiruyte ssylku" with copy button
       - Close button

       **State 2c: Requisites Display** (after selecting option 3)
       - Show QR code image (from qr_code_base64): `<img src="data:image/png;base64,{qr}" />`
       - Show card number (if exists): large text, copy button
       - Show SBP phone (if exists): large text, copy button
       - Show bank name
       - "Klient oplatil" primary button -> calls confirm endpoint -> on success, show ReceiptDataCard or toast

       Props: `{ open, booking, hasRobokassa, hasRequisites, hasSeenGreyWarning, defaultFiscalization, onClose, onPaymentComplete }`

       IMPORTANT: All actual UI text MUST be in Russian (Cyrillic). The transliterated text above is for plan readability only. Use these exact Russian strings:
       - "Способ оплаты", "Внести доход вручную", "Наличные, карта, перевод"
       - "Отправить ссылку клиенту", "СБП через Робокассу"
       - "Показать реквизиты + QR", "Клиент переведёт сам"
       - "Отметить оплату", "Наличные", "Карта", "Перевод", "СБП"
       - "Без чеков", "Ручной", "Авто"
       - "Рекомендуем выставлять чеки для защиты от штрафов", "Понятно"
       - "Оплачено", "Ссылка отправлена клиенту", "Или скопируйте ссылку"
       - "Клиент оплатил"

    3. **Create ReceiptDataCard** (frontend/src/components/ReceiptDataCard.tsx, ~60 lines):
       - Card component with receipt data for manual fiscalization
       - Shows: amount, service name, client name, date -- each as a line with label (Caption) and value (Title)
       - "Скопировать данные" button -- copies all data as formatted text to clipboard via `navigator.clipboard.writeText()`
       - "Открыть Мой Налог" link button -- opens `https://lknpd.nalog.ru/` in new tab
       - "Готово" button -- closes the sheet/card
       Props: `{ data: ManualReceiptData, onClose }`

    4. **Create PaymentHistory page** (frontend/src/pages/master/PaymentHistory.tsx, ~150 lines):
       - Heading: "Платежи" (20px semibold)
       - Filter bar: status pills (Все | Ожидает | Оплачено | Отменено) using same pill pattern as Bookings
       - Date range: two date inputs (date_from, date_to) side by side
       - Payment list: Card for each payment showing:
         - Service name (Title), client name (Caption)
         - Amount in rubles (Title, right-aligned, formatted with space thousands separator)
         - Status badge: pending (amber), paid (emerald), cancelled (red), refunded (gray)
         - Receipt status badge (smaller): "Чек: выдан" (emerald), "Чек: ожидает" (amber), "Нет чека" (gray)
         - Date + time (Caption)
         - Tap on paid payment with fiscalization=manual: show ReceiptDataCard
       - Empty state: "Платежей пока нет" + "Здесь будет история всех оплат" body
       - Pagination: "Показать ещё" button at bottom if total > items.length
       - Use usePaymentHistory hook with filter state

    5. **Extend Bookings.tsx**: Add "Завершить" button to each booking card with status "confirmed":
       - Button: secondary style, text "Завершить", positioned after the cancel button
       - On tap: open PaymentSheet with the booking data
       - Pass hasRobokassa / hasRequisites / hasSeenGreyWarning from payment settings (fetch with usePaymentSettings, see Task 2)
       - On payment complete callback: invalidate bookings query, show success toast

    6. **Register PaymentHistory route** (frontend/src/App.tsx): Add `/master/payments` route inside the master MasterLayout routes, rendering PaymentHistory page.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm/frontend && npx tsc --noEmit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - payments.ts exports 7 hooks (3 create mutations, confirm, cancel, history query, receipt query)
    - PaymentSheet renders 3 options with correct disabled states
    - Manual payment flow: select method -> confirm -> receipt card (if manual fiscalization) or toast
    - Robokassa flow: loading -> success message with link copy
    - Requisites flow: QR + card/phone + confirm button
    - ReceiptDataCard has copy and "Open Moy Nalog" buttons
    - PaymentHistory shows filterable paginated list with status + receipt badges
    - Bookings.tsx has "Zavershit" button on confirmed bookings
    - /master/payments route registered in App.tsx
    - TypeScript compiles and pnpm build succeeds
  </acceptance_criteria>
  <done>Master can complete payments via 3 methods from booking list, view payment history, and see receipt data for manual fiscalization</done>
</task>

<task type="auto">
  <name>Task 2: Settings payment configuration sections and Robokassa wizard</name>
  <files>
    frontend/src/components/RobokassaWizard.tsx
    frontend/src/api/master-settings.ts
    frontend/src/pages/master/Settings.tsx
  </files>
  <read_first>
    frontend/src/pages/master/Settings.tsx
    frontend/src/api/master-settings.ts
    frontend/src/components/ui/Button.tsx
    frontend/src/components/ui/Card.tsx
    frontend/src/components/ui/Toast.tsx
    frontend/src/lib/constants.ts
  </read_first>
  <action>
    1. **Extend master-settings.ts** with payment settings hooks:
       ```typescript
       interface PaymentSettings {
         card_number: string | null; sbp_phone: string | null; bank_name: string | null;
         has_robokassa: boolean; robokassa_is_test: boolean; robokassa_hash_algorithm: string;
         fiscalization_level: string; has_seen_grey_warning: boolean; receipt_sno: string;
       }
       interface PaymentSettingsUpdate {
         card_number?: string | null; sbp_phone?: string | null; bank_name?: string | null;
         fiscalization_level?: string; receipt_sno?: string;
       }
       interface RobokassaSetup {
         merchant_login: string; password1: string; password2: string;
         is_test: boolean; hash_algorithm: string;
       }
       ```
       Hooks:
       - `usePaymentSettings()` -- GET /settings/payment, useQuery
       - `useUpdatePaymentSettings()` -- PUT /settings/payment, mutation, invalidates ["master", "paymentSettings"]
       - `useSetupRobokassa()` -- POST /settings/payment/robokassa, mutation
       - `useDisconnectRobokassa()` -- DELETE /settings/payment/robokassa, mutation
       - `useMarkGreyWarningSeen()` -- POST /settings/payment/grey-warning-seen, mutation

    2. **Create RobokassaWizard** (frontend/src/components/RobokassaWizard.tsx, ~150 lines):
       3-step wizard rendered inline (not modal):

       **Step 1: Instructions**
       - Heading: "Подключение Робокассы"
       - Numbered instruction list in Card:
         1. "Зарегистрируйтесь на robokassa.com" (link opens in new tab)
         2. "Создайте магазин в личном кабинете"
         3. "Скопируйте данные со страницы технических настроек"
       - "Далее" button to step 2

       **Step 2: Credentials Form**
       - Heading: "Технические настройки"
       - Input fields (each h-44px, using existing Input component pattern):
         - Логин магазина (merchant_login)
         - Пароль #1 (password1, type=password)
         - Пароль #2 (password2, type=password)
       - Test mode toggle: "Тестовый режим" with a switch/checkbox
       - Hash algorithm selector: pills (MD5 | SHA-256 | SHA-512), default SHA-256
       - "Подключить" primary button -> calls useSetupRobokassa mutation
       - "Назад" secondary button

       **Step 3: Success**
       - Checkmark icon (emerald)
       - "Робокасса подключена!"
       - "Теперь вы можете отправлять ссылки на оплату через СБП"
       - "Готово" button -> onComplete callback

       Error handling: show error toast on mutation failure. Show inline validation (required fields).
       Props: `{ onComplete, onCancel }`

    3. **Extend Settings.tsx** -- add 3 new sections below existing settings, separated by lg (24px) spacing:

       **Section: Реквизиты для оплаты**
       - Card with 3 Input fields:
         - Номер карты (card_number, placeholder "2200 1234 5678 9012")
         - Телефон для СБП (sbp_phone, placeholder "+7 916 123 45 67")
         - Название банка (bank_name, placeholder "Сбербанк")
       - "Сохранить" button -> useUpdatePaymentSettings mutation
       - Caption below: "Эти данные увидит клиент при оплате"

       **Section: Робокасса**
       - If has_robokassa=false: show "Робокасса не подключена" + "Подключить" button -> expand RobokassaWizard inline
       - If has_robokassa=true: show success badge "Подключена" + "Тестовый режим" indicator (if active) + "Отключить" destructive button -> ConfirmDialog ("Отключить Робокассу? Вы не сможете отправлять ссылки на оплату." / "Отключить" / "Отмена") -> useDisconnectRobokassa

       **Section: Чеки и фискализация**
       - Pill selector for fiscalization_level: "Без чеков" | "Ручной" | "Автоматический"
         - "Автоматический" disabled if !has_robokassa (with caption "Требуется Робокасса")
         - On selecting "Без чеков" for first time (!has_seen_grey_warning): show amber warning Card: "Рекомендуем выставлять чеки самозанятым для защиты от штрафов ФНС" + "Понятно" button -> useMarkGreyWarningSeen
       - If fiscalization != "none": show receipt_sno selector as pills: "Патент" | "УСН доходы" | "ОСН"
       - "Сохранить" button -> useUpdatePaymentSettings
       - Caption: "Уровень по умолчанию. Можно изменить при каждой оплате."

       Also add a navigation link to PaymentHistory: "История платежей" card with chevron-right icon, navigates to /master/payments (use react-router-dom navigate or Link).

       Fetch payment settings: use usePaymentSettings() hook. Show Skeleton loader while loading.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm/frontend && npx tsc --noEmit && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - master-settings.ts exports 5 new hooks (usePaymentSettings, useUpdatePaymentSettings, useSetupRobokassa, useDisconnectRobokassa, useMarkGreyWarningSeen)
    - RobokassaWizard has 3 steps (instructions, form, success)
    - Settings page has 3 new sections: Requisites, Robokassa, Fiscalization
    - Robokassa section conditionally shows wizard or connected status
    - Fiscalization "Auto" pill disabled when Robokassa not connected
    - Grey warning shown only once (has_seen_grey_warning flag)
    - Receipt SNO selector visible only when fiscalization is not "none"
    - "Payment history" navigation link present in Settings
    - TypeScript compiles and pnpm build succeeds
  </acceptance_criteria>
  <done>Master can configure payment requisites, connect/disconnect Robokassa, and set fiscalization level in Settings</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify payment flow end-to-end</name>
  <files>none</files>
  <action>Human verification of the complete payment system built in Tasks 1-2. No automated work -- user tests the flow manually.</action>
  <what-built>Complete payment system: 3 payment methods (manual, Robokassa SBP link, QR requisites), payment history, fiscalization settings (3 levels), and Robokassa connection wizard. All wired from Settings config through booking completion to payment history.</what-built>
  <how-to-verify>
    1. Open the mini-app in TG or dev mode
    2. Navigate to master Settings -> scroll down to new payment sections
    3. Enter card number and SBP phone in "Requisites" section -> Save
    4. Set fiscalization to "Bez chekov" -> verify one-time warning appears -> tap "Ponyatno" -> verify warning does not appear on subsequent visits
    5. Navigate to Bookings -> find a confirmed booking -> tap "Zavershit"
    6. Verify bottom sheet shows 3 options (Robokassa option disabled if not connected)
    7. Select "Vnesti dokhod vruchnu" -> select payment method -> tap "Oplacheno"
    8. Verify success toast appears and booking status changes
    9. Navigate to Settings -> "Istoriya platezhey" -> verify the payment appears in the list
    10. Test QR requisites flow: tap "Zavershit" on another booking -> select "Pokazat rekvizity + QR" -> verify QR code and card/phone are displayed -> tap "Klient oplatil"
    11. (If Robokassa is connected) Test "Otpravit ssylku klientu" flow -> verify success message with copy link
  </how-to-verify>
  <verify>Manual human verification -- see how-to-verify steps above</verify>
  <done>User confirms all 3 payment flows work, settings save correctly, payment history shows entries, and fiscalization warnings appear as expected</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd /Users/yannovak/development/projects/master_crm/frontend && pnpm build` succeeds
- `cd /Users/yannovak/development/projects/master_crm/backend && uv run pytest tests/ -x -q` all pass
- Payment flow works end-to-end: Settings -> Bookings "Zavershit" -> PaymentSheet -> PaymentHistory
- All 3 payment methods functional
- Fiscalization settings with grey warning, per-payment override, receipt data display
</verification>

<success_criteria>
- Master can complete a booking and record payment via any of 3 methods
- PaymentSheet bottom sheet appears with correct option availability
- PaymentHistory page shows all payments with status and receipt badges
- Settings has Requisites, Robokassa, and Fiscalization sections
- RobokassaWizard completes 3-step connection flow
- Grey warning shows once then never again
- Manual receipt data is copyable and links to Moy Nalog
- pnpm build succeeds, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-payments-tax-receipts/03-03-SUMMARY.md`
</output>
