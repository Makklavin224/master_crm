---
phase: 06-web-admin-panel
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - web/src/api/clients.ts
  - web/src/api/payments.ts
  - web/src/api/settings.ts
  - web/src/pages/ClientsPage.tsx
  - web/src/pages/ClientDetailPage.tsx
  - web/src/pages/PaymentsPage.tsx
  - web/src/pages/SettingsPage.tsx
  - web/src/App.tsx
autonomous: true
requirements: [WEB-02, WEB-04, WEB-05]

must_haves:
  truths:
    - "Master can search clients by name or phone and see a sortable table"
    - "Master can click a client to see their full visit history"
    - "Master can view payment history with filters by status and date range"
    - "Payment table shows payment status and receipt status with colored tags"
    - "Master can configure schedule settings (working hours, breaks, days off)"
    - "Master can edit profile information and notification preferences"
  artifacts:
    - path: "web/src/pages/ClientsPage.tsx"
      provides: "Client list table with search"
      min_lines: 60
    - path: "web/src/pages/ClientDetailPage.tsx"
      provides: "Client detail with booking history"
      min_lines: 50
    - path: "web/src/pages/PaymentsPage.tsx"
      provides: "Payment history table with filters"
      min_lines: 80
    - path: "web/src/pages/SettingsPage.tsx"
      provides: "Settings page with schedule, notifications, payment, profile sections"
      min_lines: 150
    - path: "web/src/api/clients.ts"
      provides: "Client TanStack Query hooks"
      exports: ["useClients", "useClientDetail"]
    - path: "web/src/api/payments.ts"
      provides: "Payment TanStack Query hooks"
      exports: ["usePayments"]
    - path: "web/src/api/settings.ts"
      provides: "Settings TanStack Query hooks"
      exports: ["useSettings", "useUpdateSettings", "useNotificationSettings", "useUpdateNotificationSettings", "usePaymentSettings", "useScheduleTemplate", "useUpdateScheduleTemplate"]
  key_links:
    - from: "web/src/pages/ClientsPage.tsx"
      to: "/api/v1/clients"
      via: "useClients hook"
      pattern: "useClients"
    - from: "web/src/pages/ClientDetailPage.tsx"
      to: "/api/v1/clients/{id}"
      via: "useClientDetail hook"
      pattern: "useClientDetail"
    - from: "web/src/pages/PaymentsPage.tsx"
      to: "/api/v1/payments/history"
      via: "usePayments hook with filter params"
      pattern: "usePayments"
    - from: "web/src/pages/SettingsPage.tsx"
      to: "/api/v1/settings, /api/v1/schedule/template, /api/v1/settings/notifications"
      via: "multiple settings hooks"
      pattern: "useSettings|useScheduleTemplate|useNotificationSettings"
---

<objective>
Build the Clients, Payments, and Settings pages to complete the web admin panel.

Purpose: WEB-02 (client management), WEB-04 (payment history), and WEB-05 (schedule settings + profile) complete the full admin panel feature set.
Output: ClientsPage with search and ClientDetailPage with history, PaymentsPage with filters and status tags, SettingsPage with schedule/notifications/payment/profile sections.
</objective>

<execution_context>
@/Users/yannovak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yannovak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-web-admin-panel/06-CONTEXT.md
@.planning/phases/06-web-admin-panel/06-RESEARCH.md
@.planning/phases/06-web-admin-panel/06-01-SUMMARY.md

<interfaces>
<!-- Backend API contracts -->
From backend/app/schemas/client.py:
```python
class ClientRead(BaseModel):
    id: uuid.UUID
    phone: str
    name: str
    created_at: datetime

class MasterClientRead(BaseModel):
    client: ClientRead
    first_visit_at: datetime | None
    last_visit_at: datetime | None
    visit_count: int

class ClientDetailRead(BaseModel):
    client: ClientRead
    bookings: list[BookingRead]
    visit_count: int
```

From backend/app/api/v1/clients.py:
```
GET /clients -> list[MasterClientRead]
GET /clients/{id} -> ClientDetailRead
```

From backend/app/schemas/payment.py:
```python
class PaymentRead(BaseModel):
    id: uuid.UUID
    booking_id: uuid.UUID
    amount: int  # in kopecks
    status: str  # pending, paid, cancelled, refunded
    payment_method: str | None
    receipt_status: str  # not_applicable, pending, issued, failed, cancelled
    fiscalization_level: str | None
    paid_at: datetime | None
    created_at: datetime
    payment_url: str | None = None
    service_name: str | None = None
    client_name: str | None = None

class PaymentListResponse(BaseModel):
    items: list[PaymentRead]
    total: int
```

From backend/app/api/v1/payments.py:
```
GET /payments/history?status=&date_from=&date_to=&limit=&offset= -> PaymentListResponse
```

From backend/app/schemas/settings.py:
```python
class MasterSettings(BaseModel):
    buffer_minutes: int
    cancellation_deadline_hours: int
    slot_interval_minutes: int

class MasterSettingsUpdate(BaseModel):
    buffer_minutes: int | None  # 0-60
    cancellation_deadline_hours: int | None  # 0-72
    slot_interval_minutes: int | None  # 15 or 30

class NotificationSettings(BaseModel):
    reminders_enabled: bool
    reminder_1_hours: int
    reminder_2_hours: int | None
    address_note: str | None

class PaymentSettings(BaseModel):
    card_number: str | None
    sbp_phone: str | None
    bank_name: str | None
    has_robokassa: bool
    robokassa_is_test: bool
    fiscalization_level: str  # none, manual, auto
    has_seen_grey_warning: bool
    receipt_sno: str
```

From backend/app/api/v1/settings.py:
```
GET  /settings -> MasterSettings
PUT  /settings -> MasterSettings
GET  /settings/notifications -> NotificationSettings
PUT  /settings/notifications -> NotificationSettings
GET  /settings/payment -> PaymentSettings
PUT  /settings/payment -> PaymentSettings
POST /settings/payment/robokassa -> 200
DELETE /settings/payment/robokassa -> 204
```

From backend/app/schemas/schedule.py:
```python
class ScheduleDayEntry(BaseModel):
    day_of_week: int  # 0-6
    start_time: time
    end_time: time
    break_start: time | None
    break_end: time | None
    is_working: bool

class ScheduleTemplate(BaseModel):
    days: list[ScheduleDayEntry]  # exactly 7 entries

class ScheduleExceptionCreate(BaseModel):
    exception_date: date
    is_day_off: bool = True
    start_time: time | None
    end_time: time | None
    reason: str | None

class ScheduleExceptionRead(BaseModel):
    id: uuid.UUID
    exception_date: date
    is_day_off: bool
    start_time: time | None
    end_time: time | None
    reason: str | None
    created_at: datetime
```

From backend/app/api/v1/schedule.py:
```
GET  /schedule/template -> list[ScheduleDayRead]
PUT  /schedule/template -> (accepts ScheduleTemplate)
GET  /schedule/exceptions -> list[ScheduleExceptionRead]
POST /schedule/exceptions -> ScheduleExceptionRead
DELETE /schedule/exceptions/{id} -> 204
```

<!-- Shared components from Plan 01 -->
From web/src/components/StatusTag.tsx:
```typescript
export function BookingStatusTag({ status }: { status: string }): JSX.Element
export function PaymentStatusTag({ status }: { status: string }): JSX.Element
```

From web/src/api/client.ts:
```typescript
export async function apiRequest<T>(path: string, options?: RequestInit): Promise<T>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clients and Payments pages with API hooks</name>
  <files>
    web/src/api/clients.ts, web/src/api/payments.ts,
    web/src/pages/ClientsPage.tsx, web/src/pages/ClientDetailPage.tsx,
    web/src/pages/PaymentsPage.tsx
  </files>
  <read_first>
    frontend/src/api/master-clients.ts, frontend/src/api/payments.ts,
    backend/app/schemas/client.py, backend/app/schemas/payment.py,
    backend/app/api/v1/clients.py, backend/app/api/v1/payments.py,
    web/src/api/client.ts, web/src/components/StatusTag.tsx
  </read_first>
  <action>
    1. Create `web/src/api/clients.ts` -- TanStack Query hooks:
       - Interfaces: ClientRead, MasterClientRead, ClientDetailRead, BookingRead (for detail view).
       - `useClients()` -- GET /clients. queryKey: ["clients"]. staleTime: 30_000.
       - `useClientDetail(clientId: string)` -- GET /clients/{clientId}. queryKey: ["clients", clientId]. enabled: !!clientId.

    2. Create `web/src/api/payments.ts` -- TanStack Query hooks:
       - Interfaces: PaymentRead, PaymentListResponse, PaymentFilters.
       - `usePayments(filters: { status?: string; date_from?: string; date_to?: string; limit?: number; offset?: number })` -- GET /payments/history with query params. queryKey: ["payments", filters]. staleTime: 30_000.

    3. Create `web/src/pages/ClientsPage.tsx`:
       - Ant Design Card with title "Клиенты".
       - Search Input at top with SearchOutlined prefix, placeholder "Поиск по имени или телефону", max-width 400px.
       - Client-side filtering: filter useClients() data by search string matching name (case-insensitive) or phone.
       - Ant Design Table with columns:
         - Имя (client.name), sortable alphabetically.
         - Телефон (client.phone).
         - Визиты (visit_count), sortable numerically.
         - Первый визит (first_visit_at), render via dayjs format 'DD.MM.YYYY', sortable. Show "-" if null.
         - Последний визит (last_visit_at), render via dayjs format 'DD.MM.YYYY', sortable. Show "-" if null.
       - Row onClick: navigate to `/clients/${record.client.id}`.
       - Cursor pointer on rows.
       - rowKey: record.client.id.
       - Pagination: pageSize 20.
       - Loading state from isLoading.

    4. Create `web/src/pages/ClientDetailPage.tsx`:
       - Read clientId from URL params (useParams).
       - Use useClientDetail(clientId) hook.
       - Top section: Ant Design Descriptions (bordered) showing:
         - Имя: client.name
         - Телефон: client.phone (with copy icon using Typography.Text copyable)
         - Всего визитов: visit_count
         - В базе с: dayjs(client.created_at).format('DD.MM.YYYY')
       - Bottom section: Card with title "История визитов".
       - Ant Design Table with booking history:
         - Дата (starts_at), format 'DD.MM.YYYY HH:mm', sortable (default descending).
         - Услуга (service_name).
         - Статус (status), render using BookingStatusTag.
         - Длительность: computed from starts_at/ends_at as minutes.
       - Back button (Breadcrumb or Button with ArrowLeftOutlined) navigating to /clients.
       - Loading/error states.

    5. Create `web/src/pages/PaymentsPage.tsx`:
       - Ant Design Card with title "Платежи".
       - Filter bar at top (Space/Flex):
         - DatePicker.RangePicker for date_from/date_to (format 'DD.MM.YYYY').
         - Select for status filter: all (no filter), pending, paid, cancelled, refunded. Placeholder "Статус". allowClear.
       - Ant Design Table with columns:
         - Дата (created_at), format 'DD.MM.YYYY HH:mm', sortable (default descending).
         - Клиент (client_name).
         - Услуга (service_name).
         - Сумма (amount), render as `(amount / 100).toLocaleString('ru-RU')` + " руб".
         - Способ оплаты (payment_method), render as human-readable label: cash="Наличные", card_to_card="Перевод на карту", transfer="Перевод", sbp="СБП". Show "-" if null.
         - Статус (status), render using PaymentStatusTag.
         - Чек (receipt_status), render as Tag: not_applicable="Не требуется" (grey), pending="Ожидание" (gold), issued="Выдан" (green), failed="Ошибка" (red), cancelled="Отменён" (default).
       - Pagination with offset/limit: use total from PaymentListResponse. pageSize 20. onChange updates offset.
       - Loading state from isLoading.
       - Summary row or statistic at top: total count from response.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm/web && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ClientsPage shows table with 5 columns, search filters by name/phone
    - Clicking a client row navigates to /clients/{id}
    - ClientDetailPage shows client info + booking history table
    - PaymentsPage shows table with 7 columns including status and receipt tags
    - Date range picker and status select filter payment results
    - Prices display in rubles (divided by 100)
    - All API hooks use apiRequest from web/src/api/client.ts
    - pnpm build succeeds
  </acceptance_criteria>
  <done>ClientsPage with search + sortable table, ClientDetailPage with visit history, PaymentsPage with date/status filters and receipt status tags -- all wired to backend API via TanStack Query hooks</done>
</task>

<task type="auto">
  <name>Task 2: Settings page with schedule, notifications, payment, and profile sections</name>
  <files>
    web/src/api/settings.ts, web/src/pages/SettingsPage.tsx, web/src/App.tsx
  </files>
  <read_first>
    frontend/src/api/master-settings.ts, frontend/src/api/master-schedule.ts,
    frontend/src/pages/master/Settings.tsx, frontend/src/pages/master/Schedule.tsx,
    backend/app/schemas/settings.py, backend/app/schemas/schedule.py,
    backend/app/api/v1/settings.py, backend/app/api/v1/schedule.py,
    web/src/api/client.ts, web/src/stores/auth.ts
  </read_first>
  <action>
    1. Create `web/src/api/settings.ts` -- TanStack Query hooks:
       - Interfaces: MasterSettings, MasterSettingsUpdate, NotificationSettings, NotificationSettingsUpdate, PaymentSettings, PaymentSettingsUpdate, ScheduleDayEntry, ScheduleTemplate, ScheduleExceptionRead, ScheduleExceptionCreate.
       - `useSettings()` -- GET /settings. queryKey: ["settings"].
       - `useUpdateSettings()` -- PUT /settings mutation. Invalidates ["settings"].
       - `useNotificationSettings()` -- GET /settings/notifications. queryKey: ["notification-settings"].
       - `useUpdateNotificationSettings()` -- PUT /settings/notifications mutation. Invalidates ["notification-settings"].
       - `usePaymentSettings()` -- GET /settings/payment. queryKey: ["payment-settings"].
       - `useUpdatePaymentSettings()` -- PUT /settings/payment mutation. Invalidates ["payment-settings"].
       - `useScheduleTemplate()` -- GET /schedule/template. queryKey: ["schedule-template"].
       - `useUpdateScheduleTemplate()` -- PUT /schedule/template mutation. Invalidates ["schedule-template"].
       - `useScheduleExceptions()` -- GET /schedule/exceptions. queryKey: ["schedule-exceptions"].
       - `useCreateScheduleException()` -- POST /schedule/exceptions mutation. Invalidates ["schedule-exceptions"].
       - `useDeleteScheduleException()` -- DELETE /schedule/exceptions/{id} mutation. Invalidates ["schedule-exceptions"].

    2. Create `web/src/pages/SettingsPage.tsx` with Ant Design Tabs (left tabPosition for desktop feel):
       - 4 tabs: Расписание, Уведомления, Платежи, Профиль.

       **Tab 1 -- Расписание (Schedule):**
       - "Booking settings" section: Ant Design Form with:
         - buffer_minutes: Select with options 0/5/10/15/30/60 мин. Label "Буфер между записями".
         - cancellation_deadline_hours: Select with options 0/1/2/4/6/12/24/48 ч. Label "Дедлайн отмены".
         - slot_interval_minutes: Radio.Group with 15/30 мин. Label "Интервал слотов".
         - Save button. On submit: useUpdateSettings mutation. Show success notification.

       - "Weekly schedule" section: Ant Design Table (not Form, but editable) showing 7 rows (Mon-Sun):
         - День (day name in Russian: Пн, Вт, Ср, Чт, Пт, Сб, Вс).
         - Рабочий (Switch for is_working).
         - Начало (TimePicker for start_time, disabled if !is_working).
         - Конец (TimePicker for end_time, disabled if !is_working).
         - Перерыв (TimePicker.RangePicker for break_start/break_end, disabled if !is_working).
         - Load initial values from useScheduleTemplate().
         - Save button at bottom. Collects all 7 entries into ScheduleTemplate.days array. Calls useUpdateScheduleTemplate.

       - "Exceptions" section: small table of schedule exceptions:
         - Дата, Тип (выходной/изменённый график), Причина, Delete button.
         - Add button opens a simple Modal with DatePicker, Switch (is_day_off), optional TimePickers for start/end, reason Input.
         - Uses useScheduleExceptions, useCreateScheduleException, useDeleteScheduleException.

       **Tab 2 -- Уведомления (Notifications):**
       - Ant Design Form with:
         - reminders_enabled: Switch. Label "Автоматические напоминания".
         - reminder_1_hours: Select with options 1/2/6/12/24 ч. Label "Первое напоминание". Disabled if !reminders_enabled.
         - reminder_2_hours: Select with options 1/2/6/12/24 ч or null (None). Label "Второе напоминание". Disabled if !reminders_enabled.
         - address_note: Input.TextArea, 3 rows. Label "Адрес/примечание (добавляется к напоминаниям)".
         - Save button. Uses useUpdateNotificationSettings.

       **Tab 3 -- Платежи (Payment):**
       - Read-only display of payment configuration (PaymentSettings):
         - Реквизиты section: card_number, sbp_phone, bank_name displayed in Descriptions or list.
         - Робокасса section: show connected status (has_robokassa), test mode indicator.
         - Фискализация section: show current fiscalization_level as Badge.
         - Note: "Для изменения настроек платежей используйте мини-приложение" (payment config changes are complex, keep in mini-app for now -- the web panel shows read-only status).
         - This is intentional: the Robokassa wizard, payment requisites form, and fiscalization level selector are complex multi-step flows already built in the mini-app. Duplicating them in the web panel adds significant scope for v1. The web panel shows current status.

       **Tab 4 -- Профиль (Profile):**
       - Display master info from GET /auth/me (useQuery for master profile):
         - Имя (name), Email (email), Телефон (phone), Название бизнеса (business_name), Часовой пояс (timezone).
         - Displayed in Ant Design Descriptions (bordered).
         - Note: Master profile editing is not in v1 requirements. Display only for now.

    3. Verify App.tsx has all routes: /clients/:id for ClientDetailPage, /payments for PaymentsPage, /settings for SettingsPage. Update if needed.
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm/web && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SettingsPage has 4 tabs: Расписание, Уведомления, Платежи, Профиль
    - Schedule tab: booking settings form (buffer, deadline, interval) saves to API
    - Schedule tab: weekly schedule table with 7 rows, TimePickers for hours/breaks, save button
    - Schedule tab: exceptions list with add/delete functionality
    - Notifications tab: form with reminders toggle, interval selects, address note
    - Payments tab: read-only display of payment configuration status
    - Profile tab: display master info from /auth/me
    - All settings hooks use apiRequest from web/src/api/client.ts
    - App.tsx routes include /clients/:id, /payments, /settings
    - pnpm build succeeds
  </acceptance_criteria>
  <done>SettingsPage with 4 tabs (schedule editing with weekly template and exceptions, notification preferences, payment status display, profile info), ClientsPage/ClientDetailPage/PaymentsPage all routed and functional</done>
</task>

</tasks>

<verification>
1. `cd web && pnpm build` exits 0
2. ClientsPage filters by search input
3. ClientDetailPage shows booking history table
4. PaymentsPage filters by date range and status
5. SettingsPage has 4 functional tabs
6. All routes in App.tsx resolve to correct pages
</verification>

<success_criteria>
- Clients page: searchable table, click-through to client detail with visit history
- Payments page: filterable table showing status and receipt tags, prices in rubles
- Settings page: editable schedule (weekly template + exceptions), notification preferences, payment status display, profile info
- All pages consume existing backend API via TanStack Query hooks
- Complete admin panel: all 5 sidebar menu items lead to functional pages
</success_criteria>

<output>
After completion, create `.planning/phases/06-web-admin-panel/06-03-SUMMARY.md`
</output>
