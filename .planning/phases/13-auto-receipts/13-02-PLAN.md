---
phase: 13-auto-receipts
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - frontend/src/api/master-settings.ts
  - frontend/src/pages/master/Settings.tsx
  - web/src/api/settings.ts
  - web/src/pages/SettingsPage.tsx
autonomous: true
requirements: [ARCT-01]

must_haves:
  truths:
    - "Mini-app Settings shows INN input and bind/disconnect buttons in fiscalization section"
    - "Web admin Settings Payments tab shows INN status and auto-receipts connection state"
    - "Binding INN requires Robokassa to be connected (button disabled otherwise)"
    - "After binding INN, settings show 'Avtocheki podklyucheny' with disconnect option"
  artifacts:
    - path: "frontend/src/pages/master/Settings.tsx"
      provides: "INN binding UI in mini-app settings"
      contains: "inn"
    - path: "web/src/pages/SettingsPage.tsx"
      provides: "INN status in web admin payments tab"
      contains: "inn"
    - path: "frontend/src/api/master-settings.ts"
      provides: "useBindInn and useUnbindInn mutation hooks"
      contains: "useBindInn"
    - path: "web/src/api/settings.ts"
      provides: "useBindInn and useUnbindInn mutation hooks"
      contains: "useBindInn"
  key_links:
    - from: "frontend/src/pages/master/Settings.tsx"
      to: "frontend/src/api/master-settings.ts"
      via: "useBindInn/useUnbindInn hooks"
      pattern: "useBindInn"
    - from: "web/src/pages/SettingsPage.tsx"
      to: "web/src/api/settings.ts"
      via: "useBindInn/useUnbindInn hooks"
      pattern: "useBindInn"
---

<objective>
Frontend INN binding UI in both mini-app and web admin settings pages: INN input field, bind/disconnect buttons, auto-receipt status display, and API hooks for POST/DELETE /settings/payment/inn.

Purpose: Masters can activate auto-receipts by entering their 12-digit INN in settings when Robokassa is connected.
Output: Updated Settings pages with INN binding section in both mini-app and web admin.
</objective>

<execution_context>
@/Users/yannovak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yannovak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-auto-receipts/13-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on. -->

Backend endpoints created in Plan 01:
```
POST   /api/v1/settings/payment/inn    — Body: { "inn": "123456789012" } → PaymentSettings
DELETE /api/v1/settings/payment/inn    — No body → PaymentSettings
GET    /api/v1/settings/payment        — Returns PaymentSettings (now includes inn, fns_connected)
```

PaymentSettings schema (after Plan 01):
```typescript
interface PaymentSettings {
  card_number: string | null;
  sbp_phone: string | null;
  bank_name: string | null;
  has_robokassa: boolean;
  robokassa_is_test: boolean;
  robokassa_hash_algorithm: string;
  fiscalization_level: string;    // "none" | "manual" | "auto"
  has_seen_grey_warning: boolean;
  receipt_sno: string;
  inn: string | null;             // NEW — 12-digit INN
  fns_connected: boolean;         // NEW — auto-receipts active
}
```

From frontend/src/api/master-settings.ts (existing hooks):
```typescript
export function usePaymentSettings(): UseQueryResult<PaymentSettings>;
export function useUpdatePaymentSettings(): UseMutationResult;
export function useSetupRobokassa(): UseMutationResult;
export function useDisconnectRobokassa(): UseMutationResult;
```

From web/src/api/settings.ts (existing hooks):
```typescript
export function usePaymentSettings(): UseQueryResult<PaymentSettings>;
export function useUpdatePaymentSettings(): UseMutationResult;
```

UI patterns from frontend/src/pages/master/Settings.tsx:
- PillButton for option selectors
- Card component for sections
- Button component with loading state
- useToast() for success/error messages
- ConfirmDialog for destructive actions

UI patterns from web/src/pages/SettingsPage.tsx:
- Ant Design Card, Descriptions, Badge, Button
- App.useApp() for message API
- Tabs with tabPosition="left"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mini-app Settings -- INN binding section + API hooks</name>
  <files>
    frontend/src/api/master-settings.ts
    frontend/src/pages/master/Settings.tsx
  </files>
  <read_first>
    frontend/src/api/master-settings.ts
    frontend/src/pages/master/Settings.tsx
    frontend/src/components/ui/Card.tsx
    frontend/src/components/ui/Badge.tsx
    frontend/src/components/ConfirmDialog.tsx
  </read_first>
  <action>
1. **master-settings.ts** -- add inn and fns_connected to PaymentSettings interface, add hooks:
   - Add `inn: string | null` and `fns_connected: boolean` to `PaymentSettings` interface
   - Add `useBindInn()` mutation hook:
     ```typescript
     export function useBindInn() {
       const queryClient = useQueryClient();
       return useMutation({
         mutationFn: (inn: string) =>
           masterApiRequest<PaymentSettings>("/settings/payment/inn", {
             method: "POST",
             body: JSON.stringify({ inn }),
           }),
         onSuccess: () => {
           queryClient.invalidateQueries({ queryKey: ["master", "paymentSettings"] });
         },
       });
     }
     ```
   - Add `useUnbindInn()` mutation hook:
     ```typescript
     export function useUnbindInn() {
       const queryClient = useQueryClient();
       return useMutation({
         mutationFn: () =>
           masterApiRequest<PaymentSettings>("/settings/payment/inn", {
             method: "DELETE",
           }),
         onSuccess: () => {
           queryClient.invalidateQueries({ queryKey: ["master", "paymentSettings"] });
         },
       });
     }
     ```

2. **Settings.tsx** -- add "Nalogi i cheki" (Tax receipts) section AFTER the fiscalization Card, BEFORE the payment history link:
   - Import `useBindInn`, `useUnbindInn` from master-settings
   - Add state: `const [innInput, setInnInput] = useState("")`
   - Add state: `const [showUnbindDialog, setShowUnbindDialog] = useState(false)`
   - Initialize hooks: `const bindInn = useBindInn()`, `const unbindInn = useUnbindInn()`
   - Read from paymentSettings: `const fnsConnected = paymentSettings?.fns_connected ?? false`, `const masterInn = paymentSettings?.inn ?? null`

   - New Card section:
     ```
     <Card className="flex flex-col gap-3">
       <h2>Налоги и чеки</h2>
       {fnsConnected ? (
         <!-- Connected state -->
         <div>
           <Badge variant="confirmed">Авточеки подключены</Badge>
           <p>ИНН: {masterInn}</p>
           <Button variant="destructive" onClick={() => setShowUnbindDialog(true)}>
             Отключить
           </Button>
         </div>
       ) : (
         <!-- Not connected state -->
         <div>
           {!hasRobokassa ? (
             <p>Для автоматических чеков подключите Робокассу</p>
           ) : (
             <div>
               <label>ИНН (12 цифр)</label>
               <input value={innInput} onChange={...} maxLength={12} pattern="\d*" inputMode="numeric"
                 placeholder="123456789012" className={same style as other inputs} />
               <Button onClick={handleBindInn} loading={bindInn.isPending}
                 disabled={innInput.length !== 12}>
                 Подключить авточеки
               </Button>
               <p className="text-[12px] text-text-secondary">
                 Чеки будут автоматически отправляться в ФНС через Робокассу
               </p>
             </div>
           )}
         </div>
       )}
     </Card>
     ```

   - `handleBindInn` function:
     ```typescript
     const handleBindInn = () => {
       bindInn.mutate(innInput, {
         onSuccess: () => { toast.success("Авточеки подключены"); setInnInput(""); },
         onError: () => toast.error("Не удалось подключить авточеки"),
       });
     };
     ```

   - `handleUnbindInn` function:
     ```typescript
     const handleUnbindInn = () => {
       unbindInn.mutate(undefined, {
         onSuccess: () => { toast.success("Авточеки отключены"); setShowUnbindDialog(false); },
         onError: () => { toast.error("Не удалось отключить авточеки"); setShowUnbindDialog(false); },
       });
     };
     ```

   - Add ConfirmDialog for unbind (same pattern as Robokassa disconnect dialog):
     ```
     <ConfirmDialog
       isOpen={showUnbindDialog}
       title="Отключить авточеки?"
       message="Чеки не будут автоматически отправляться в ФНС."
       confirmLabel="Отключить"
       cancelLabel="Отмена"
       variant="destructive"
       onConfirm={handleUnbindInn}
       onCancel={() => setShowUnbindDialog(false)}
     />
     ```

   - INN input must only accept digits (filter non-digits on onChange): `onChange={(e) => setInnInput(e.target.value.replace(/\D/g, "").slice(0, 12))}`
   - Input styling: same as existing inputs (h-[44px], rounded-[10px], border, etc.)
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm && grep -c "useBindInn\|useUnbindInn" frontend/src/api/master-settings.ts && grep -c "Налоги и чеки\|fns_connected\|handleBindInn" frontend/src/pages/master/Settings.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PaymentSettings interface in master-settings.ts includes inn and fns_connected
    - useBindInn hook POSTs to /settings/payment/inn
    - useUnbindInn hook DELETEs /settings/payment/inn
    - Settings.tsx has "Налоги и чеки" Card section
    - When fns_connected: shows "Авточеки подключены" badge + INN + disconnect button
    - When not connected + has Robokassa: shows INN input + bind button
    - When not connected + no Robokassa: shows "подключите Робокассу" message
    - INN input accepts only 12 digits
    - ConfirmDialog for unbinding exists
  </acceptance_criteria>
  <done>Mini-app Settings shows INN binding section with connect/disconnect flow matching existing UI patterns</done>
</task>

<task type="auto">
  <name>Task 2: Web admin Settings -- INN status in Payments tab + API hooks</name>
  <files>
    web/src/api/settings.ts
    web/src/pages/SettingsPage.tsx
  </files>
  <read_first>
    web/src/api/settings.ts
    web/src/pages/SettingsPage.tsx
  </read_first>
  <action>
1. **settings.ts** -- add inn and fns_connected to PaymentSettings interface, add hooks:
   - Add `inn: string | null` and `fns_connected: boolean` to `PaymentSettings` interface
   - Add `useBindInn()` mutation hook:
     ```typescript
     export function useBindInn() {
       const qc = useQueryClient();
       return useMutation({
         mutationFn: (inn: string) =>
           apiRequest<PaymentSettings>("/settings/payment/inn", {
             method: "POST",
             body: JSON.stringify({ inn }),
           }),
         onSuccess: () => qc.invalidateQueries({ queryKey: ["payment-settings"] }),
       });
     }
     ```
   - Add `useUnbindInn()` mutation hook:
     ```typescript
     export function useUnbindInn() {
       const qc = useQueryClient();
       return useMutation({
         mutationFn: () =>
           apiRequest<PaymentSettings>("/settings/payment/inn", { method: "DELETE" }),
         onSuccess: () => qc.invalidateQueries({ queryKey: ["payment-settings"] }),
       });
     }
     ```

2. **SettingsPage.tsx** -- extend PaymentsTab with INN/auto-receipts section:
   - Import `useBindInn`, `useUnbindInn` from settings API
   - Import `Input, Popconfirm` from antd (Input already imported, add Popconfirm if not present)
   - Add state to PaymentsTab: `const [innInput, setInnInput] = useState("")`
   - Initialize hooks: `const bindInn = useBindInn()`, `const unbindInn = useUnbindInn()`
   - Get messageApi from `App.useApp()`

   - After the existing "Фискализация" Badge row in the Descriptions, add a new row:
     ```
     <Descriptions.Item label="Авточеки (ФНС)">
       {data.fns_connected ? (
         <>
           <Badge status="success" text="Подключены" />
           <span style={{ marginLeft: 8 }}>ИНН: {data.inn}</span>
           <Popconfirm
             title="Отключить авточеки?"
             description="Чеки не будут автоматически отправляться в ФНС"
             onConfirm={handleUnbindInn}
           >
             <Button type="link" danger size="small" style={{ marginLeft: 8 }}>
               Отключить
             </Button>
           </Popconfirm>
         </>
       ) : data.has_robokassa ? (
         <div style={{ display: "flex", gap: 8, alignItems: "center" }}>
           <Input
             value={innInput}
             onChange={(e) => setInnInput(e.target.value.replace(/\D/g, "").slice(0, 12))}
             placeholder="ИНН (12 цифр)"
             maxLength={12}
             style={{ width: 180 }}
           />
           <Button
             type="primary"
             size="small"
             onClick={handleBindInn}
             loading={bindInn.isPending}
             disabled={innInput.length !== 12}
           >
             Подключить
           </Button>
         </div>
       ) : (
         <span style={{ color: "#888" }}>Требуется подключение Робокассы</span>
       )}
     </Descriptions.Item>
     ```

   - `handleBindInn` callback:
     ```typescript
     const handleBindInn = useCallback(async () => {
       try {
         await bindInn.mutateAsync(innInput);
         messageApi.success("Авточеки подключены");
         setInnInput("");
       } catch {
         messageApi.error("Не удалось подключить авточеки");
       }
     }, [bindInn, innInput, messageApi]);
     ```

   - `handleUnbindInn` callback:
     ```typescript
     const handleUnbindInn = useCallback(async () => {
       try {
         await unbindInn.mutateAsync();
         messageApi.success("Авточеки отключены");
       } catch {
         messageApi.error("Не удалось отключить авточеки");
       }
     }, [unbindInn, messageApi]);
     ```

   - The note at the bottom "Для изменения настроек платежей используйте мини-приложение" remains -- web admin now also allows INN binding directly
  </action>
  <verify>
    <automated>cd /Users/yannovak/development/projects/master_crm && grep -c "useBindInn\|useUnbindInn" web/src/api/settings.ts && grep -c "Авточеки\|fns_connected\|handleBindInn" web/src/pages/SettingsPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PaymentSettings interface in settings.ts includes inn and fns_connected
    - useBindInn and useUnbindInn hooks exist in settings.ts
    - PaymentsTab shows "Авточеки (ФНС)" row in Descriptions
    - When fns_connected: shows success badge + INN + disconnect Popconfirm
    - When not connected + has_robokassa: shows INN input + connect button
    - When not connected + no robokassa: shows "Требуется подключение Робокассы"
    - INN input accepts only 12 digits
  </acceptance_criteria>
  <done>Web admin Settings Payments tab shows INN status with bind/unbind functionality</done>
</task>

</tasks>

<verification>
1. Mini-app Settings: "Налоги и чеки" section visible with correct state-dependent UI
2. Web admin Settings > Payments: "Авточеки (ФНС)" row visible in Descriptions
3. Both UIs: INN input accepts only 12 digits, bind button disabled until 12 digits entered
4. Both UIs: After binding, shows connected state with disconnect option
5. API hooks invalidate payment-settings query on success
</verification>

<success_criteria>
- Master can bind INN from mini-app settings when Robokassa connected (ARCT-01 frontend)
- Master can bind INN from web admin settings when Robokassa connected (ARCT-01 frontend)
- Connected state shows "Avtocheki podklyucheny" with INN and disconnect option
- Disconnected state prompts to connect Robokassa if not connected, or shows INN input if connected
</success_criteria>

<output>
After completion, create `.planning/phases/13-auto-receipts/13-02-SUMMARY.md`
</output>
